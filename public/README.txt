Drop static assets here.

Required for the homepage:
- dashboard-hero.png — A screenshot of your /landlord dashboard for the hero image.
  Recommended size: ~1200x900 px (4:3 ratio). The image is object-fit: cover
  so any size works; just keep the important UI in the top-left.

Without this file, a placeholder block appears in the hero. The site still functions.
