# Rentletter second audit, 7 September 2026

Read only. Repository at commit fd583dd3947719c59efc1634a3d4ce9878f25f1c, 36 commits after the first audit (docs/audit-2026-09.md at 1e77c96). Written for a solo founder who ships through Claude Code and tests on an iPhone. Every claim carries a file and a line. GUESS marks an inference. UNKNOWN marks what the repository cannot tell, with what would resolve it.

What was run: the unit suite (147 tests, 147 pass, tests/*.test.mjs, 24 files), headless Chrome at 390 wide against the sandbox on a local dev server (screenshots under /tmp/walk-*.png), and reads of every file cited. Not run: a build, SQL, production Supabase, KV, Resend, the Vercel crons, iOS Safari.

Two facts that frame everything below. First, the four policies the whole realtor side depends on (listings_all_own, listing_applicants_all_own, profiles_select_own, profiles_update_own) exist in the repository only as a comment export headed "Reference only, do not run" (db/schema-reference.sql:61 to :62, policies at :143 to :146). Whether they exist in production is UNKNOWN. Second, almost every feature since 1e77c96 was verified by unit tests and the sandbox, not by a real session against production; the sandbox mirrors the routes in lib/demoAdapter.js and skips every limiter, session and ownership check by construction.

## Part 1. What the builds left behind

### 1a. Dead code

Method: an import graph over pages, components, lib and tests (every `from '...'` and `import(...)`), exported names checked for any reference outside their own file, api routes checked for a fetch of their path from any non api file, plus targeted greps. Line counts from `wc -l`.

Files nothing imports.

| File | Lines | Who used it | Safe to delete |
|---|---|---|---|
| components/dashboard/ProfileEditorModal.js | 27 | GUESS: the pre onboarding profile modal; no importer anywhere | yes |
| components/film/ProductFilmLazy.js | 14 | GUESS: a lazy wrapper for the product film that pages/index.js no longer mounts | yes |
| lib/scoring.js | 98 | lib/scorecard.js (calculateScorecard) and lib/deriveScorecard.js (rentAffordabilityScore, overallScore) | not yet, see below |
| lib/scorecard.js | 62 | pages/api/generate.js:226 and lib/tenantProfile.js:200 still call calculateScorecard and store the result in the scorecard jsonb; no realtor or tenant surface reads scorecard.overall or its components (grep of `scorecard.` across components, pages and lib returns only lib/pipelinePrefill.js:13, the ageConfirmed flag) | the computation is dead weight; the column write survives only because age_confirmed rides inside it (1b) |
| lib/deriveScorecard.js | 71 | withLiveScore is called by lib/supabaseBridge.js:114 and lib/demoAdapter.js:100. It re derives scorecard.rentAffordability from the live rent and recomputes rent_to_income_ratio (lib/deriveScorecard.js:66 to :70). The ratio half is read by the card (components/dashboard/ListingView.js:690), Compare (:614), the synthesis line (lib/applicantSynthesis.js:41) and the snapshot fallback (lib/reportSnapshot.js:56). The scorecard half is read by nothing | keep the ratio derivation (five lines), drop the scorecard derivation and lib/scoring.js with it |

Routes no client fetches.

| Route | Lines | Note |
|---|---|---|
| /api/applicants/clear-analysis | 65 | manage-analysis (delete action) replaced it; no component fetches the path |
| /api/pass/create, /api/pass/verify | 10, 9 | answer 410; the paid letter is gone |
| /api/promos/validate | 21 | pages/join/[code].js calls lib/promos.js directly (GUESS from the grep; confirm by reading pages/join/[code].js) |
| /api/cron/*, /api/tenant/verify, /api/tenant/verify-email-change | | reached by Vercel and by email links, not dead |

Exports never referenced outside their file (57 names across 46 lib files). The ones worth removing rather than keeping as documentation: lib/adminData.js planFounderRenumber and renumberFounders (the renumber is invoked through pages/api/admin/realtors.js by string action; UNKNOWN whether it still is: read that route), lib/billing.js ensureCustomer, applySubscription, endSubscription (GUESS: the webhook now calls a single entry point), lib/referrals.js CONSENT_TTL, saveReferral, listToEmail, listForApplicant, verificationSummary (the referral system is paused behind lib/features.js:9), lib/nudges.js selectNudge and nudgeEmail (used only through runNudges and the tests), lib/docRequest.js newDocReqToken, PENDING_KEY, kvSadd (used inside the module), lib/pipeline.js newPrefillToken, prefillKey, ownedConsent, selectRenewals (module internal plus tests), lib/tenantProfileStore.js getProfileById, factsFromApplication, findApplicationNumbersByEmail. The full list is in the sweep output; none of them is a route or a component.

Demo adapter mirrors: every `'METHOD /api/...'` key in lib/demoAdapter.js has a matching file under pages/api today (checked by path). lib/dashboardAdapter.js DASHBOARD_ROUTES lists GET /api/assistant/signals, GET /api/assistant/dismiss and POST /api/assistant/dismiss, which no component fetches by path (lib/assistantStore.js fetches them, so they are live; the contract check happens to pass through lib).

Event types: every EVENT_TYPES entry is recorded somewhere. verification_completed and verification_failed are written through a computed `outcome` variable (lib/realtorUpload.js:93, pages/api/upload/finalize.js:87), which a literal grep misses.

KV keys with a writer and no reader in the product: `events:letters` and the `counters` (lib/stats.js, written from pages/api/generate.js:315 to :316) are read only by pages/admin/stats.js, the founder's page, so they are live. `auditlog:{RL}` is written and read inside pages/api/application/manage.js. No orphan prefix was found. The `referrals` Supabase table is written by lib/referrals.js:68 and never read anywhere (KV is canonical); it is a write only mirror.

Column writes nothing reads: `age` and `date_of_birth` are no longer written (717bdd0); `reason_for_moving`, `ev_parking_needed`, `personality`, `vehicle_make_model`, `vehicle_year`, `disclosures`, `cover_letter` are written as null on every mirror by lib/applicationMap.js:43 to :63 and read by nothing. `number_of_occupants` and `occupants_details` are written (lib/applicationMap.js:54 to :55) and read only by the tenant's own editor (components/tenant/ProfileFacts.js:195) and the paused referral facts sheet (lib/referrals.js:99). `scorecard` is written on every submit and edit and read only for ageConfirmed (above).

Homepage form: pages/index.js still contains the full application form (sections at :825 to :906) behind `step === 'form'` (:735), entered from :137 ("jump straight to form") and :283. It asks Age and Date of birth (:839 to :840), which pages/api/generate.js now ignores (the destructuring at :151 to :168 no longer names them), and Total occupants and Other occupants (:870, :882), which generate stores. GUESS: this form is reached through the apply by code path on the homepage; whether any live link still lands there is UNKNOWN (grep the marketing pages for `setStep('form')` triggers and check analytics).

Leftovers of removed features still in code: the referral surfaces (components/dashboard/ReferModal.js, ReferralCaution.js, ReferralInbox.js, lib/referrals.js 284 lines, lib/referralEmails.js, six routes under pages/api/referrals, the growth loop copy in pages/signup.js:158) all dark behind lib/features.js:9; the `ai_insight` key in lib/docVerifications.js:28 and :56; `pref_max_occupants` is gone from code (last touched 1f30d8c); `longTermIntent` is gone (d3ef69b).

### 1b. Hacks

| Hack | Where | What breaks and when | Proper fix |
|---|---|---|---|
| age_confirmed lives inside scorecard jsonb | lib/applicationMap.js:66 writes `scorecard.ageConfirmed`; lib/pipelinePrefill.js:13 and lib/tenantProfile.js:120 read it back | The scorecard object is recomputed by lib/tenantProfile.js buildApplicationFromForm (line 203) and by pages/api/generate.js (line 231) as the output of calculateScorecard; the flag survives only because kvAppToRow merges `tenant.ageConfirmed` in on every mirror. A future write of the applications row that does not go through kvAppToRow drops it silently. It also means a scoring artefact carries a legal check | A boolean column `age_confirmed` on applications (a one line migration) read by the same three lines |
| The retention event can never be recorded | lib/retention.js:94 calls recordEvent with `profileId: profileIdForEvent`, default null (lib/retention.js:64); pages/api/cron/retention.js:21 never passes one; lib/events.js:19 returns false when profileId is missing | In enforce mode a deletion run leaves no timeline entry anywhere. The catch at lib/retention.js:94 never fires because recordEvent returns false instead of throwing, so the log line at :92 is the only record | Either an admin scoped event (a `retention_run` row per affected profile) or a separate `retention_runs` table; the event type as declared cannot carry a run that spans profiles |
| The pending nudge set is a KV set with no owner | lib/docRequest.js:80 to :87 (docreq-pending); members added at mint (lib/docRequest.js comment :13 to :15), removed at finalize (pages/api/upload/finalize.js), at set aside and withdraw (lib/realtorWrites.js:59, :81), at status change (:141) and by the cron when a decision says drop (lib/nudges.js:102) | Set aside and withdraw remove the member at once (lib/realtorWrites.js:59, :81), so the "waits a day" concern from the brief is closed since 1f30d8c. What remains: a member whose pointer key expired (7 days, lib/docRequest.js:107) stays in the set until the cron reads a null pointer and drops it (lib/nudges.js:28, :102); the set grows between runs and every run does one SMEMBERS plus one MGET over the whole set (lib/nudges.js:79 to :81) | Keep the pointer and the pending marker in one place: a `docs_requested_at` column on listing_applicants already queried by the cron, or a KV sorted set keyed by request time so the cron reads only the due members |
| The second nudge can carry a dead link | lib/docRequest.js:106 sets `docreq:{token}` with DOCREQ_TTL, 7 days, at mint; lib/nudges.js:110 rewrites only the app pointer (`kv.appKey`) with a fresh TTL; nudge two is due at 5 days (lib/nudges.js:14, :40) | A cron that runs late, or a request minted at 13:01 UTC, sends nudge two at day 6 with an upload link that dies at day 7. The tenant who opens the email on day 8 sees "This link is no longer active" (pages/upload/[token].js, seen in the walk) | Extend `docreq:{token}` on each nudge, or mint the request for 14 days and nudge inside the first week |
| The signals cache is per function instance | lib/signalsCache.js:8 module level Map, 60 second TTL (:7); every write route calls invalidateSignals for the caller | On Vercel two warm instances hold two caches; a write on one leaves the badge stale up to 60 seconds on the other. Documented as acceptable at lib/signalsCache.js:4 to :6. It becomes wrong when a route relies on it for correctness: none does today | Leave it, or move it to KV with the same TTL if the badge is ever shown as a count that must be right |
| Sandbox tokens are accepted by production routes | pages/api/pipeline/answer.js:25, pages/api/pipeline/consent.js:22 and :27, pages/api/references/answer.js:26, pages/api/invite/resolve.js:14, pages/api/applications/mirror.js:27, pages/api/report/answer.js:22 and :27, pages/api/report/pdf.js:17, pages/keep/[token].js:18, pages/ref/[token].js:19, pages/r/[token].js:18 | Each of these branches runs on production traffic. They write nothing (each returns a canned 200 or 4xx), but they are product code shaped for the sandbox, and the rate limiter is consulted before the demo branch in answer.js (:23) and consent.js (:25), so a demo token can burn a real IP's hourly budget | One `isSandboxToken()` helper in lib/features.js and a single early return in each route, or serve the sandbox pages from lib/demoAdapter.js only |
| The `?profile=fresh` switch in the sandbox | lib/demoAdapter.js (search `profile=fresh`) reads window.location.search inside createDemoAdapter | Harmless, but it is a test affordance living in product code and persists into sessionStorage for the rest of the sandbox session | A sandbox settings drawer, or nothing |
| The archive entry keeps an `ai_insight` key | lib/docVerifications.js:28 and :56 write `ai_insight: null` into every archived report entry | Nothing breaks; the key is a fossil of the cut feature inside the doc_verifications jsonb | Leave it (renaming a stored shape key is a migration) |
| The invite record and the listing row both hold the invite | pages/api/listings/invite.js:102 writes `linvite:{token}` to KV (90 day TTL, :109) and :117 writes invite_token and invite_url on the listing row through the session client | After 90 days the KV record expires while the row still carries the token: pages/api/invite/resolve.js:29 answers 404 "expired" for a link the dashboard still shows; pages/api/generate.js:191 then stores estimated_rent null for that submission | Treat the listings row as the source of truth: resolve from the row (it already carries address, rent, bedrooms, province through the profile) and keep KV only for rate limiting |
| Province asked at sign up and again in the listing modal | pages/signup.js:200 to :202 asks the province and carries it in user metadata (:104); components/listings/ListingSetupModal.js asks it when the profile has none (askProvince) | Not a break. A realtor who signed up through the form never sees the modal field; only a profile with province null does. Two places own one fact | Pick one: drop the sign up field now that the modal asks just in time |

### 1c. Seams

Label vocabulary. Fit produces five labels (lib/fitScore.js:139: verified, check docs, stated, docs match). The other surfaces restate them in their own words.

| Surface | What it shows | Line | Agrees |
|---|---|---|---|
| Card header | the Fit label in caps (VERIFIED, CHECK DOCS, STATED, DOCS MATCH) and a muted meter for stated and check docs | components/dashboard/ListingView.js:678 | yes |
| Card state line | "Documents on file · nothing matched", "Name on documents did not match", "Profile edited … · analyse again", "Verified by you" | components/dashboard/ListingView.js:778 to :813 | partly: the state line speaks in sentences, the label in two words; "checked" state shows label CHECK DOCS above "Documents on file · nothing matched" |
| Listing summary line | "1 verified · 2 to check · 1 waiting on documents · 2 no documents" | lib/listingStateLine.js:17 and :27 to :29 | partly: "to check" collapses checked, mismatch and edited, which the card shows as three different lines |
| Next list and bell | kinds check_docs, mismatch, edited, verify with titles "Documents differ", "Name did not match", "Edited after documents", "Verify {first}" | lib/actions.js:29, :51 to :55 | partly: "Documents differ" is a fourth wording for the same checked state |
| Landlord page and PDF | the label in caps with the rank; the reason line "Below the one above: documents did not match" | pages/r/[token].js:129; lib/fitScore.js:175 (fitReason) | yes for the label; the reason line says "documents did not match" where the card says "nothing matched" |
| Text report | the label through the snapshot payload | lib/reportText.js reads payload.applicants[].fit.label | yes |
| Checklist "Docs:" facts | "name matches", "did not match", "matched", "not matched" | components/dashboard/ScreeningChecklist.js:65 to :67 | partly: a fifth vocabulary (matches, matched, not matched) on the same screen as the card's label |

Confirmation keys.

| Surface | Keys it knows | Line | Agrees |
|---|---|---|---|
| Fit | id, employer, landlord, landlord_reference, reference | lib/fitScore.js:79, :122 | yes |
| Confirm route | id, employer, landlord, reference (landlord_reference is written only by the answer route) | pages/api/applicants/confirm.js:15; lib/referenceStore.js:64 | yes by design |
| Checklist | id, employer, landlord, reference as tappable rows; landlord_reference shown as the answers line, not as a Confirmed pill | components/dashboard/ScreeningChecklist.js rows at :72 to :77 | partly: an emailed reference does not turn the Previous landlord row's own control to "Confirmed", so the row can read "Answered Sep 5" beside an untapped "Called landlord" |
| Report confirmed line | all five; landlord_reference labelled "previous landlord (by email)" | lib/reportSnapshot.js:22 | yes |
| Report "Confirmed by" name | the first non reference `by`, else "the realtor" | lib/reportSnapshot.js:26 | seam: David Kowalski's card on the landlord page reads "Confirmed by the realtor: previous landlord (by email)" while Priya's reads "Confirmed by Sarah Chen" (walk, /tmp/walk-13-landlord-page.png). Same page, two names for one person |
| Applicant state | employer only (verified state), id for name match | lib/applicantState.js:42 to :46 | yes by design |
| Sandbox confirm route | id, employer, landlord, reference | lib/demoAdapter.js:307 | yes |
| People | whatever the junction row carries, through computeFit | lib/pipelineState.js:51 | yes |

Invite source of truth: the KV record `linvite:{token}` (pages/api/listings/invite.js:102) versus the listings row (invite_token at :117). Readers of KV: pages/api/invite/resolve.js:29, pages/api/invite/tag.js:27, pages/api/generate.js:191, pages/api/pipeline/consent.js:31. Readers of the row: pages/api/invite/resolve.js:42 (status only), pages/api/applications/mirror.js:56, pages/api/listings/invite.js:53 and :84. Disagreement: the two have different lifetimes (90 days versus forever) and the row's `invite_url` is a frozen string while the short link (lib/shortLink.js) is minted later; see 1b.

Emails: the sender and footer comparison is in Part 2b.

Sandbox versus production, routes that skip a check.

| Sandbox route | Production check skipped | Line |
|---|---|---|
| every route in lib/demoAdapter.js | session, entitlement, ownership, rate limit (by construction) | lib/demoAdapter.js:116 to :318 |
| POST /api/applicants/analyze-file (sandbox) | the 4MB decoded cap is mirrored (413) but the MAX_DOCS limit and the mime check are not | lib/demoAdapter.js:243 to :251 |
| POST /api/references/request (sandbox) | the 5 day resend rule is mirrored; the "no email on file" refusal is mirrored; nothing sends | lib/demoAdapter.js:293 to :304 |
| POST /api/listings/send-report (sandbox) | returns preview true, writes a snapshot into state only | lib/demoAdapter.js:233 |
| GET /api/listings/report-pdf and report-text (sandbox) | proxied to real routes under pages/api/demo which build from the fixture with no session | lib/demoAdapter.js:231 to :232; pages/api/demo/*.js |
| answer routes with demo tokens (production code) | the limiter runs first, the write is skipped | pages/api/pipeline/answer.js:23 to :28; pages/api/references/answer.js:24 to :30; pages/api/report/answer.js:22 to :27 |

### 1d. Tests

See the classification table at the end of this part (1d table).

#### 1d table. Tests: behaviour versus source text

Counts. 147 tests in 24 files, all passing. 941 `assert.*` call sites by grep; 138 of them inspect source text (readFileSync, existsSync, a directory walk, or a parsed vercel.json or db file) and 803 call code and check outputs or state. One test is empty: tests/events.test.mjs:17 to :18 has no body. Only tests/features.test.mjs:24, :29 and :36 import a pages/api handler and call it (the six referral routes); every other route named below is reached only through a text match.

Every source text assertion, and whether it passes while the behaviour is broken:

| test file:line | source file inspected | what it checks | passes while broken? |
|---|---|---|---|
| tests/hygiene.test.mjs:72 | pages/api/applicants/analyze-documents.js | existsSync is false | NO: in the Pages Router the file is the route |
| tests/hygiene.test.mjs:74 | components/dashboard/ApplicantDocIntel.js | no `analyze-documents`; has `/api/applicants/analyze-file`, `/api/applicants/finalize-analysis`, `3MB each` | YES: strings only; the request body and the cap value are not pinned |
| tests/hygiene.test.mjs:97 | lib/actions.js | no `duplicate` | YES: an item worded "Same phone as" carries no such word |
| tests/hygiene.test.mjs:99 | lib/supabaseBridge.js | `markDuplicates(byListing[id])` present | YES: the call can exist with its result discarded |
| tests/hygiene.test.mjs:115 | pages/api/generate.js | no `dateOfBirth`, no `age: age`, has `ageConfirmed: ageConfirmed === true` | YES: a key under another name, or `age,` shorthand, slips through |
| tests/hygiene.test.mjs:117 | pages/apply/[token].js | `dateOfBirth: undefined, age: undefined, ageConfirmed:` | YES: a later spread could reintroduce the field |
| tests/hygiene.test.mjs:126 | pages/api/generate.js | no `apartmentDescription.match`; has the rentFromInvite call | YES: pins one line, not the guard or the use of the value |
| tests/hygiene.test.mjs:128 | lib/tenantProfile.js | no `description).match` | YES: a differently written parse slips through |
| tests/hygiene.test.mjs:138 | components/listings/ListingSetupModal.js | `needsRentConfirm(initial, buildPayload(), activeApplicants)` | YES: whether the result gates the save is not pinned |
| tests/hygiene.test.mjs:140 | components/dashboard/CompareTenants.js | no `occupants` | YES: a column labelled "Household size" slips through |
| tests/legacyRemoved.test.mjs:14 to :16 | pages/api/landlord, pages/shortlist, pages/api/download.js | existsSync false | NO: absence is the behaviour |
| tests/legacyRemoved.test.mjs:23 | every file | no legacy path string | YES: a path assembled at runtime slips through |
| tests/legacyRemoved.test.mjs:27 to :34 | pages/api/invite/resolve.js, tag.js, apply page, index | files exist, strings present | YES: handlers never called |
| tests/features.test.mjs:69 | 13 files | `referralsEnabled()` present | YES for the seven UI files (the six routes are behaviour tested at :21 to :41) |
| tests/cuts.test.mjs:16, :29 | four chat files, the insight route | existsSync false | NO: absence is the behaviour |
| tests/cuts.test.mjs:20 to :25, :33 to :39 | every file, AssistantPanel, supabaseBridge, docVerifications | no chat or insight identifiers; TABS literal; OPTIONAL_JUNCTION_COLS literal; `ai_insight: null` | YES: renamed identifiers or a tab rendered outside TABS slip through |
| tests/cuts.test.mjs:50 to :57 | pages/onboarding.js, OnboardingFlow, pages/landlord.js | the redirect line, the write literal, no old steps, the gate line | YES: an earlier return can make the pinned branch unreachable; nothing runs getServerSideProps |
| tests/cuts.test.mjs:68 to :77 | ListingSetupModal, HomeView, ListingView | askProvince default, the province spread, provinceOk, the profile fetch body, the signature block, the sheet props, the brand hint condition | YES: props, destructuring and body literals only; no component renders |
| tests/references.test.mjs:43 | pages/ref/[token].js | no textarea or type="text" | YES: a contentEditable element slips through |
| tests/references.test.mjs:56 to :57 | pages/api/references/request.js | withRealtor, ownedApplicant, copy, event name; no `application.email` | YES: presence only; `const { email } = application` slips through |
| tests/references.test.mjs:88 to :91 | pages/ref/[token].js, pages/api/references/answer.js | no `.update(` before export default; readRequest call; limiter and event names | YES: a write inside an imported helper slips through (readRequest itself is behaviour tested at :67) |
| tests/references.test.mjs:115, :124, :126 | lib/fitScore.js, lib/reportSnapshot.js, lib/landlordReportPdf.js | no answer field names | YES: a read under another key slips through; the runtime Fit check at :112 to :113 is what covers it |
| tests/pipeline.test.mjs:77 | lib/pipelineState.js | the computeFit call text | YES: the behaviour test at :66 to :75 covers it |
| tests/pipeline.test.mjs:138 to :139 | pages/apply/[token].js | getServerSideProps exists, reads `ctx.query?.from`, calls readPrefill, has `data-invited-review` and the prefill route string | YES: the result can be dropped; the review can be hidden |
| tests/pipeline.test.mjs:176 to :179 | pages/api/pipeline/answer.js, pages/keep/[token].js | `Renewal(admin, ` present (matches readRenewal and answerRenewal alike); the renewed copy; no `.update(` before export default | YES: a page calling the writing helper passes |
| tests/pipeline.test.mjs:205 to :215 | invite and remove routes, lib/pipeline.js, lib/dashboardAdapter.js, vercel.json, db/pipeline.sql | wrapper and call names; route strings; the cron schedule; SQL fragments | YES: the SQL file is not the deployed schema; the routes are never invoked |
| tests/documents.test.mjs:78 to :80 | open.js, delete.js, confirm.js | requireEntitlement present, ownedApplicant textually after it, no `supabase.from('listings')` | YES: text order, not execution order |
| tests/documents.test.mjs:124 to :125 | expire-documents.js, vercel.json | cronGate before expireDocuments textually; the schedule | YES: the gate's return can be ignored |
| tests/nudges.test.mjs:92 to :108 | lib/docRequest.js, finalize.js, status.js, lib/nudges.js, mirror.js, request-documents.js, add-applicant.js, lib/referrals.js, vercel.json, the two tenant pages, DocumentUploader | kvSadd, kvSrem, mintRequest call sites, URL strings, the schedule | YES: call sites only; mirror.js:70 to :78 wraps the mint in a try/catch that swallows failure |
| tests/reportSnapshot.test.mjs:67 to :79 | pages/r/[token].js, lib/reportSnapshotStore.js, pages/api/report/answer.js | the notFound returns, the expired state, the noteOpened call, the opened_count expression, the hourly guard, the limiter, the answer merge, the event name, invalidateSignals | YES: an earlier branch can bypass; calls can be unawaited |
| tests/reportSnapshot.test.mjs:106, :112 to :113 | report-text.js, send-report.js | no Anthropic; pageUrl assignment; buildLandlordReportPdf call | YES: reportEmail (:25 to :50) is never called; the PDF can be built and not attached |
| tests/shortLink.test.mjs:21 to :37 | pages/a/[code].js, pages/api/listings/invite.js | kvGet call, redirect literal, two copy strings, the shortCode assignment, the KV set and expire URLs, the del, the reuse expression | YES: pages/api/listings/invite.js:99 to :100 never check those fetch responses |
| tests/listingStatus.test.mjs:18 to :22, :91 to :92, :119 | pages/api/listings/status.js, pages/keep/[token].js, pages/api/pipeline/answer.js | getUser, requireEntitlement, ownedListing, admin client present; no session update; readConsent call; the limiter; the method literal | YES: presence only |
| tests/events.test.mjs:14 | db/events.sql | EVENT_TYPES equals the parsed CHECK list | YES: the SQL file, not the deployed constraint |
| tests/applicationIds.test.mjs:42 to :43 | seven files | no `[A-F0-9]{4}`; imports lib/applicationIds | YES: `[0-9A-F]{4}` slips through |
| tests/realtorWrites.test.mjs:112 to :113 | every component and page | no `.from('x')...insert|update|upsert|delete|rpc` within 160 chars; more than 40 files scanned | YES for :112 (a browser write through a lib helper slips through); :113 guards the scan itself |

Five behaviours with no test that a paying realtor hits first.

1. Sign in, session, entitlement, the dashboard load. pages/landlord.js getServerSideProps (:32 to :88: the sign in redirect at :38 to :40, the profile retry at :21 to :29, suspension at :49 to :52, the province backfill at :57, promo redemption at :60 to :63, getEntitlement at :82, loadSignals at :70) never runs in a test. No test drives the trial branch of lib/entitlements.js:34 to :41 or the paid and past_due branches at :25 to :33; tests/realtorWrites.test.mjs:24 and :26 use plan none and plan founding only. pages/signin.js:31 and pages/auth/callback.js:21 have no test.
2. Creating a listing and getting the invite link. pages/api/listings/create.js is never invoked (tests/realtorWrites.test.mjs:68 calls the lib function with a fake admin). pages/api/listings/invite.js:19 to :125 (the KV record at :102 to :106, the short code writes at :99 to :100 whose responses are never checked, the row update under RLS at :117, the event at :118) is covered by five text matches only.
3. The tenant submit path. pages/api/generate.js:136 to :327 and pages/api/applications/mirror.js:18 to :86 (the legitimacy check at :41 to :44, the upsert at :59, the link at :63, the applicant_applied event at :65, the document request mint at :74) are never called; lib/supabaseBridge.js:18 to :47 (the upsert with the PGRST204 retry) has no test.
4. The listing page load. pages/landlord/[id].js:31 to :66 (the RLS listing read at :43, the redirect at :47 to :49, fetchListingApplicants at :55, attachDocVerifications at :58, latestSnapshots at :60) never runs; lib/supabaseBridge.js:150 and :163 have no test.
5. Sending the report. pages/api/listings/send-report.js:52 to :104 (the 503 without RESEND_API_KEY at :56, loadReportContext at :69, the landlord email check at :72 to :73, reportEmail at :82, the send with the PDF attached at :84 to :88, last_sent_at at :94, the event at :96) is never called; reportEmail (:25 to :50) and lib/listingReportData.js:150 have no test.

Close behind: the realtor upload routes as routes (pages/api/applicants/analyze-file.js:20 to :31 and finalize-analysis.js:16 to :25 are never invoked; tests/hygiene.test.mjs:29 to :75 drives the lib with a fake `analyze` and a fake `store`), and the tenant upload routes (pages/api/upload/analyze-file.js:28 to :128, finalize.js:25 onward). That gap is exactly where the live bug in Part 2e sits.

## Part 2. Security and privacy, every route

86 files under pages/api. Shared helpers: lib/realtorRoute.js withRealtor (405 at :22, session at :25 to :26, entitlement at :28), lib/realtorAuth.js requireRealtor (session at :9 to :10, no entitlement, returns supabase, user, profile at :12), lib/requireEntitlement.js (:12 reads the own profile, :16 answers 402), lib/adminAuth.js requireAdmin (:68, KV `asession:{sha256(cookie)}` at :65), lib/ownApplicant.js ownedApplicant (junction :8, listing.profile_id compare :13), lib/listingStatus.js ownedListing (:14), lib/applicantAnalysis.js authorizeApplicant (listing read through the session client :27 to :29, junction through admin scoped to that listing :30 to :35, owner_token deleted :38), lib/rateLimit.js checkSubmitLimits (:15, 10 per token per hour, 30 per IP, fail open :22), lib/tenantProfileStore.js sessionProfile (:232, KV `tsession:{hash}`), lib/documentStore.js cronGate (:153 to :158), lib/supabaseBridge.js (owner_token and cover_letter deleted at :112 to :113).

| File | Methods (gate line) | Session | Entitlement | Ownership | Rate limit | Reads | Writes | Returns | Notes |
|---|---|---|---|---|---|---|---|---|---|
| admin/crm.js | GET, POST (:21, 405 at :24) | requireAdmin :19 | admin gate | founder only | NONE | crm_brokerages, crm_leads, crm_notes | crm tables insert, update, delete; KV admin_audit | CRM rows; `{ ok, lead or brokerage or note }` | UUID check on delete ids only (:27); store errors echoed (:40) |
| admin/login.js | POST (:7) | mints the session | NONE | public | 5 per 15 min per IP (lib/adminAuth.js:39) | env ADMIN_PASSWORD | KV `asession:` 1 year, HttpOnly cookie, admin_audit | `{ ok }` | constant time compare (lib/adminAuth.js:35) |
| admin/logout.js | POST (:3) | reads own cookie | NONE | N/A | NONE | KV | KV del own session | `{ ok }` | public mutation of the caller's own session only |
| admin/overview.js | no method gate | requireAdmin :6 | admin gate | N/A | NONE | profiles, listings, listing_applicants, applications, tenant_profiles, auth users, KV audit | NONE | every realtor's email and plan | no 405 |
| admin/promos.js | GET, POST (:15 to :16) | requireAdmin :12 | admin gate | N/A | NONE | promo tables, profiles | promo_codes insert and revoke | codes and availability | thrown messages echoed (:24) |
| admin/realtors.js | POST (:14) | requireAdmin :15 | admin gate | founder acts on any account; delete needs every email typed (:29 to :31) | NONE | profiles, listings, junction, auth | hard delete of accounts and their data, storage logos, KV invite keys; suspend | counts | ids capped at 50 |
| admin/session.js | GET (:9) | requireAdmin :10 | NONE | N/A | NONE | KV | NONE | `{ ok }` | |
| applicants/analyze-file.js | POST (withRealtor) | withRealtor | withRealtor | lib/realtorUpload.js ownedApplicant :19 to :27 | NONE per request; MAX_DOCS staged (lib/realtorUpload.js:46, :54) | listings, junction with application, KV `rstage:` | KV `rstage:`; bucket plus applicant_documents rows; one Anthropic call | `{ ok, filename, documentType, staged }` | body 6mb (:18). See 2e: the store call passes `data`, the store expects `bytes`, nothing is stored |
| applicants/clear-analysis.js | POST (:15) | getUser :21 | requireEntitlement :24 | authorizeApplicant :33 plus applicationId binding :42 | NONE | listings (RLS), junction | doc_verifications null (:50 to :53) | `{ ok, cleared }` | no client fetches this route |
| applicants/confirm.js | POST (:19) | getUser :23 | requireEntitlement :26 | ownedApplicant :34 | NONE | junction, listings, applications.full_name | confirmations (:43), events (:51) | `{ ok, confirmations }` | |
| applicants/decision.js | POST (withRealtor) | withRealtor | withRealtor | ownedApplicant in lib/realtorWrites.js:44 | NONE | junction, listings, applications | decision columns (:53), events, KV srem | `{ ok, linkId, changedAt, patch }` | |
| applicants/doc-request-status.js | GET (:11) | getUser :17 | NONE | authorizeApplicant :27 | NONE | listings (RLS), junction, KV pointer | NONE | the tenant's upload token, URL and email to the owning realtor | by design |
| applicants/finalize-analysis.js | POST (withRealtor) | withRealtor | withRealtor | lib/realtorUpload.js ownedApplicant :78 | NONE | KV `rstage:`, junction, applicant_documents | doc_verifications (lib/realtorUpload.js:86), KV del, events | `{ result, verifications, saved, held }` | |
| applicants/manage-analysis.js | POST (:24) | getUser :30 | requireEntitlement :33 | authorizeApplicant :44 plus binding :53 | NONE | listings (RLS), junction | doc_verifications (:70), docs_verified false (:79) | `{ ok, docVerifications, docArchived }` | |
| applicants/request-documents.js | POST (:22) | getUser :29 | requireEntitlement :32 | authorizeApplicant :41 plus binding :48 | NONE | listings (RLS), junction, profiles, KV pointer | KV docreq keys and pending set (lib/docRequest.js:106 to :108), events, email to the tenant with reply_to realtor (:95, :98) | token, url, status, tenantEmail | no cap on repeat sends to one tenant (renew mints a new token each time, :66) |
| applicants/reviewed.js | POST (withRealtor) | withRealtor | withRealtor | ownedApplicant (lib/realtorWrites.js:92) | NONE | junction, listings | reviewed_at (:95) | `{ ok, reviewedAt, skipped }` | |
| applicants/verify-confirm-pdf.js, verify-confirm-text.js | POST (:16, :13) | getUser | requireEntitlement | loadApplicantVerification (lib/listingReportData.js:84, binding :86) | NONE | listings (RLS), junction, profiles | NONE | PDF bytes; `{ text }` | |
| applicants/withdraw.js | POST (withRealtor) | withRealtor | withRealtor | ownedApplicant (lib/realtorWrites.js:72) | NONE | junction, listings, applications | withdrawn_at (:76), events, KV srem | `{ ok, linkId, withdrawnAt }` | |
| application/manage.js | POST (:53) | owner token compared constant time to KV `app:{RL}` (:71 to :77) | tenant route | the token names the RL | update: KV `profile_update:{RL}:{hour}` 20 per hour (:117 to :122, fail open); view, prefill, revoke, unrevoke: NONE | KV `app:`, `auditlog:` | KV `app:` (:138, :167, :177), applications upsert (:146), events, tenant profile KV, referral revoke | facts, form, `{ ok, revoked }` | RL format checked (:62); the owner token is compared but not format checked (isOwnerToken not imported) |
| applications/mirror.js | POST (:19) | none | NONE | public; RL must be in KV `invite_submissions:{token}` (:41 to :44) | NONE | KV, listings by invite_token (:53 to :57), junction, profiles | applications upsert (:59), junction upsert (:63), applicant_applied event on EVERY call (:65), docreq mint (:74) | `{ ok, mirrored, linked, docRequest }` | public write without a limiter; repeat POSTs insert repeat events |
| assistant/dismiss.js | GET, POST (:18) | getUser :21 | requireEntitlement :24 (GET too) | own KV key | NONE | KV `assistant:{id}` | KV 90 days (:39) | `{ dismissed, lastOpenedKeys }` | a locked realtor cannot read their own dismissals |
| assistant/signals.js | GET (:10) | requireRealtor :11 | NONE | listings selected by profile_id under RLS (lib/dashboardSignals.js:29) | NONE | listings, events, junction, profiles, KV, pipeline_consents | in memory cache | `{ signals, profile }` with owner_token stripped | |
| billing/checkout.js, billing/portal.js | POST (:12, :11) | requireRealtor | NONE by design | own profile | 10 per user per 10 min, 20 per IP | profiles (RLS) | stripe_customer_id via admin (lib/billing.js:18); Stripe | `{ url }` | checkout.js:19 comment says service role, :20 uses the session client |
| branding/generate-logo.js | POST (:141) | getUser :146 | requireEntitlement :149 | own profile | KV `logogen:{id}:{day}` 20 per day (:172 to :177, fail open) | profiles | one Anthropic call | `{ variations, used, limit }` | refineFrom accepts 12000 chars of client text into the prompt (:113) |
| branding/use-logo.js | POST (:19) | getUser :25 | requireEntitlement :28 | storage path and row are user.id (:57, :77) | NONE | none | bucket logos, profiles.logo_url, events | `{ logo_url, profile }` | |
| cron/expire-documents.js, nudges.js, pipeline.js, retention.js | GET (:15, :18, :17, :14) | cronGate bearer | NONE | N/A | NONE | see 2c | see 2c | counts | bearer compared with `!==` (lib/documentStore.js:157), not constant time; all four log lines say `[cron/expire-documents]` when the secret is unset (:155) |
| demo/report-pdf.js, report-text.js, verify-pdf.js, verify-text.js | GET | none | NONE | fixture only | NONE | lib/demoFixture.js | NONE | PDF bytes, `{ text }` | public CPU work per call, no limiter |
| documents/delete.js | POST (:16) | getUser :20 | requireEntitlement :23 | ownedApplicant :31 | NONE | junction, listings, applicant_documents | storage remove, deleted_at and deleted_by (lib/documentStore.js:77 to :79), events | `{ ok, deleted, deletedAt, deletedBy }` | |
| documents/open.js | POST (:17) | getUser :22 | requireEntitlement :24 | ownedApplicant :35 plus doc.profile_id === user.id :37 | NONE | applicant_documents, junction, listings | opened_count and last_opened_at (:43), events (:45), 60 second signed URL (:39) | `{ url, mime, kind }` | |
| events/index.js | GET (:9) | requireRealtor :10 | NONE | events and event_reads read through the session client (:14 to :16); scope is the policy | NONE | events, event_reads | NONE | `{ events, lastReadAt, nextBefore }` | :21 to :24 converts any error into an empty timeline |
| events/read.js | POST (:7) | requireRealtor :8 | NONE | upsert keyed on ctx.user.id (:11) | NONE | none | event_reads via admin | `{ ok, lastReadAt }` | |
| generate.js | POST (:137) | none | NONE | public | checkSubmitLimits :146 (invite token plus IP) | KV `linvite:` when the token is well formed (:190 to :191) | KV `app:{RL}` 1 year (:18 to :35), counters (:315 to :316) | `{ resume, applicationNumber, ownerToken, mode }` | stores whatever email and phone the client sends, presence checked only (:173) |
| invite/resolve.js | GET (:10) | none | NONE | public | NONE | KV `linvite:` (:29), listings status (:42) | NONE | realtor name, brokerage, listing name, unit, province | token format checked (:18); no limiter |
| invite/tag.js | POST (:8) | none | NONE | public | NONE | KV `linvite:` (:27) | KV lpush `invite_submissions:{token}` trimmed to 200 (:38 to :46), KV set `linvite:` with counters (:49 to :58) | `{ ok }` | public write with no limiter; the RL is matched against `/^RL-[A-Z0-9-]+$/i` (:15), never looked up; the SET at :51 rewrites the invite record without an EXPIRE, so under Redis semantics the 90 day TTL is dropped (UNKNOWN whether Upstash preserves it; check the provider) |
| listings/add-applicant.js | POST (:18) | getUser :24 | requireEntitlement :27 | listing read through the session client (:37 to :42) | NONE | listings (RLS), KV `app:{RL}` (:44) | applications upsert (:50), junction (:51), events | `{ ok, applicationNumber }` | any signed in realtor who knows an RL can attach that application to their listing (add by number, by design) |
| listings/applicants.js | GET (:11) | getUser :17 | NONE | listing read through the session client (:24 to :29) | NONE | listings, junction, documents, reference_responses, snapshots, KV | NONE | applicants with owner_token stripped | |
| listings/create.js | POST (withRealtor) | withRealtor | withRealtor | profile_id from the session (lib/realtorWrites.js:105) | NONE | none | listings insert, events | `{ ok, listing }` | |
| listings/delete.js | POST (withRealtor) | withRealtor | withRealtor | ownedListing (lib/realtorWrites.js:136) | NONE | listings, junction | status closed then delete (:138, :143), events, KV srem | `{ ok, listingId }` | storage objects of held documents are never removed (2e) |
| listings/invite.js | POST (:20) | getUser :24 | requireEntitlement :27 | listing read and update through the session client (:33 to :38, :117) | NONE | listings, profiles, KV | KV `short:` (:99), `linvite:` 90 days (:102 to :112), listings.invite_token and invite_url (:117), events | `{ ok, token, url, shortCode, shortUrl }` | the last session client write in the realtor path |
| listings/report-pdf.js | GET (:15) | getUser :21 | requireEntitlement :24 | loadReportContext (listing read through the session client, lib/listingReportData.js:151 to :153) | NONE | listings, profiles, applicants | events report_generated (:44) | PDF bytes | a GET that writes an event |
| listings/report-text.js | POST (:17) | getUser :20 | requireEntitlement :23 | loadReportContext :28 | NONE | as above plus latest snapshot | events | `{ text }` with the landlord page URL and token (:33) | |
| listings/send-report.js | POST (:54) | getUser :59 | requireEntitlement :62 | loadReportContext :69 | NONE | listings, profiles, applicants | report_snapshots insert with a new token, email to the landlord with the PDF attached and reply_to realtor (:84 to :88), last_sent_at (:94), events | `{ ok, sentTo, snapshot, pageUrl }` | no cap on repeat sends; each inserts a 90 day snapshot and emails |
| listings/status.js | POST (:24) | getUser :28 | requireEntitlement :31 | ownedListing :39; rentedLinkId must be on this listing :45 | NONE | listings, junction with applications | status columns (:49), KV srem, events, pipeline_consents insert per recipient (:74), emails to every non selected applicant (:81) | counts | rented, active, rented again re emails the same people (no dedupe on applicant_not_selected) |
| listings/update.js | POST (withRealtor) | withRealtor | withRealtor | ownedListing (lib/realtorWrites.js:118) | NONE | listings | whitelisted fields (:122), events | `{ ok, listing }` | |
| notifications.js | GET, POST (:30, 405 at :39) | getUser :27 | NONE | own listings under RLS; own profile row (:32) | NONE | listings, profiles, junction, applications | profiles.notifications_last_seen | items; `{ ok }` | answers 200 to anonymous callers when Supabase is unconfigured (:21 to :24) |
| pass/create.js, pass/verify.js | POST | none | NONE | N/A | NONE | none | NONE | 410 | retired |
| pipeline/answer.js | POST (:16) | consent or renew token, format 8 to 128 chars (:19) | NONE | the token names one row | checkSubmitLimits :23 | pipeline_consents | consented_at and status (lib/listingStatus.js:49) or the renewal patch (lib/pipeline.js:180) | `{ ok, status, renewed }` | sandbox branch at :25 after the limiter |
| pipeline/consent.js | POST (:19) | none | NONE | invite token must resolve in KV (:33) | checkSubmitLimits :25 | KV `linvite:`, listings by invite_token | pipeline_consents insert consented (:39) | `{ ok, message }` | any holder of a live invite link can insert consent rows for any email, no verification of that address |
| pipeline/invite.js | POST (withRealtor) | withRealtor | withRealtor | ownedConsent and ownedListing (lib/pipeline.js:77, :79) | NONE | pipeline_consents, listings, applications | KV `prefill:` 14 days (lib/pipeline.js:89), invites (:93), events, email with reply_to realtor (:31) | `{ ok, invitedAt, emailed, prefilled }` | |
| pipeline/people.js | GET (:10) | requireRealtor :11 | NONE | consents filtered by profile_id (lib/pipeline.js:31) | NONE | listings (RLS) | NONE | `{ people: [] }` always | BUG: :14 passes `ctx.admin`, which requireRealtor never returns (lib/realtorAuth.js:12); readPeopleSource returns null (lib/pipeline.js:30) and the route always answers an empty list. Callers: components/dashboard/HomeView.js (the client retry path). The server load path through lib/dashboardSignals.js:41 is unaffected |
| pipeline/prefill.js | POST (:8) | prefill token format (:10) | NONE | public | NONE | none | KV del `prefill:` | `{ ok }` always | |
| pipeline/remove.js | POST (withRealtor) | withRealtor | withRealtor | ownedConsent plus delete filtered by profile_id (lib/pipeline.js:115, :117) | NONE | pipeline_consents | delete, events | `{ ok }` | |
| profile/branding.js, profile/update.js | POST (withRealtor) | withRealtor | withRealtor | own row, whitelisted fields (lib/realtorWrites.js:27 to :28, :155) | NONE | none | profiles, events | `{ ok, profile, skipped }` | logo_url accepts any 500 char string (:185) |
| promos/redeem.js | POST (:10) | getUser :12 | NONE by design | rpc keyed to own id (:15) | 10 per IP per minute (:14) | promo_codes | rpc redeem_promo_code | `{ ok, status, message }` | always 200 |
| promos/validate.js | POST (:12) | none | NONE | public | 10 per IP per minute (:16) | promo_codes | NONE | validity, recipient first name, grant type | 180ms floor on failures (:14 to :18) |
| references/answer.js | POST (:19) | reference token, 8 to 128 chars (lib/referenceStore.js:10) | NONE | the token names one row | checkSubmitLimits :24 | reference_responses, profiles, junction, applications | answers and status (lib/referenceStore.js:59), confirmations.landlord_reference (:64), events | realtor name, status | a public token holder writes a confirmation onto the realtor's row: the feature |
| references/request.js | POST (withRealtor) | withRealtor | withRealtor | ownedApplicant :20 | one pending per 5 days (lib/referenceStore.js:23 to :29) | junction, listings, applications.prev_landlord_contact, reference_responses | reference_responses insert 14 days (:30), events, email with reply_to realtor (:35) | `{ ok, response, emailed }` | sends to an address the tenant typed, unverified, by design |
| referrals/assign.js, claim.js, create.js, inbox.js, list.js, consent.js | see notes | requireRealtor (consent: token only) | assign, create: requireEntitlement | assign: session listing read plus referral addressed to the user (lib/referrals.js:253); create: session listing read plus linkId on it | create: 10 per realtor per hour, 3 per applicant per day, 5 per receiving email per day (lib/referrals.js:133 to :135); others none | KV referral keys, `app:`, auth listUsers first 200 (lib/referrals.js:153) | KV, referrals table mirror, applications, emails | referral objects; create returns `hasAccount` for an arbitrary email (:42) | all answer 410 while lib/features.js:9 is false; inbox and list have no method gate; consent.js is a public GET that returns an applicant's facts to a token holder with no limiter (dark while paused) |
| report/answer.js | POST (:18) | report token (:22) | NONE | the token names one row | checkSubmitLimits :29 (after the sandbox branch :27) | report_snapshots | answers (:40), events landlord_answered (:42) | `{ ok, rank, answer, at }` | |
| report/pdf.js | GET (:13) | report token (:18) | NONE | public | NONE | report_snapshots (:20) | NONE | PDF with every applicant's name, income and score | does not bump opened_count (noteOpened is not called), so PDF opens are uncounted; public CPU work per call |
| send.js | POST (:164) | none | NONE | public | NONE | none | email from hello@rentletter.ca to any address in the body (:184 to :190) | `{ success }` | public, unauthenticated, unlimited sender. `applicationNumber` and `ownerToken` are interpolated into the HTML unescaped (:68, :105, :110) with no format check (:171 checks presence only; lib/applicationIds.js is not imported); `fullName` is split and interpolated (:183, :43). Anyone can make rentletter.ca email arbitrary addresses with attacker controlled fragments in those slots. Callers: pages/apply/[token].js:395, pages/index.js:290 |
| stripe/webhook.js | POST (:13) | Stripe signature (:17, constant time in lib/stripe.js:46) | NONE | N/A | NONE | stripe_events, profiles by customer | stripe_events, profiles subscription columns (lib/billing.js:65 to :83) | `{ received, handled, duplicate }` | |
| tenant/prefill.js | GET (:6) | tenant cookie session (:7) | NONE | own profile | NONE | KV profile keys | NONE | the form facts | |
| tenant/profile.js | GET, POST (:92, 405 at :97) | tenant cookie session (:89) | NONE | own profile; link-application also requires the application's email to equal the session email (:118) | request-email-change: 3 per profile per hour and 10 per IP (:127); update-facts, link-application, cancel, sign-out: NONE | KV profile, `app:`, applications, junction, listings, profiles | KV profile, `temail:`, session del, email to the new address (:138) | GET: the profile including every linked application's ownerToken (lib/tenantProfileStore.js:154, :293) | owner tokens go to the signed in tenant by design |
| tenant/request-link.js | POST (:13) | none | NONE | public | 3 per email and 10 per IP per 15 min (:19) | KV profile keys, applications by email | tenant profile KV, KV `tmagic:` 15 min (:26), email (:31) | generic `{ ok, message }` | |
| tenant/sync-application.js | POST (:14) | owner token compared constant time (:19) | NONE | the token | NONE | KV `app:` | tenant profile KV, applications.tenant_profile_id | `{ ok, email }` | owner token not format checked |
| tenant/verify.js, tenant/verify-email-change.js | no method gate | single use token consumed (lib/tenantProfileStore.js:205, :246) | NONE | N/A | NONE | KV | verify: 30 day session and cookie (:12); email change: profile re keyed (:8) | 302 | GETs that mutate; a mail scanner consumes the link before the tenant |
| upload/analyze-file.js | POST (:29) | docreq token, `/^[a-f0-9]{32}$/` (:34), record in KV (:46) | NONE | the token maps to one applicant, binding at :72 | NONE | KV docreq and staging, junction with application, listings | KV staging (:121), bucket plus applicant_documents rows (:105), one Anthropic vision call per request (:87) | `{ ok, filename, index, documentType, staged }` | public, no limiter, and NO cap on staged files (the realtor path caps at MAX_DOCS, lib/realtorUpload.js:46 and :54; this route only skips an exact name plus size repeat, :55). A token holder can run unlimited paid vision calls and bucket uploads for 7 days |
| upload/finalize.js | POST (:26) | docreq token (:30), record (:32) | NONE | two key binding :59 | NONE | KV, junction with application, listings | doc_verifications (:78), docs_submitted_at and docs_verified (:95), events, KV status and pointer, srem, del staging (:113 to :119) | `{ ok, received, verified }` | idempotent after success (:39) |
| upload/resolve.js | GET (:8) | docreq token (:11) | NONE | public | NONE | KV `docreq:` | NONE | tenant name, listing, realtor, status | |

Public routes and what they change: send.js sends email to any address with unescaped body fragments (highest priority); invite/tag.js pushes any RL string into a KV list and rewrites the invite record; applications/mirror.js upserts an application and inserts an event on every call; generate.js mints an application and an owner token (limited); application/manage.js edits, revokes and re mirrors with a valid owner token (view, revoke and unrevoke unlimited); tenant/sync-application.js attaches an application to a tenant profile with the token; tenant/verify.js and verify-email-change.js consume a token on GET; upload/analyze-file.js and finalize.js store files, run analysis and write the junction row with a docreq token, no limiter, no file count cap; pipeline/answer.js, pipeline/consent.js, references/answer.js, report/answer.js write by token (limited); pipeline/prefill.js deletes a KV key; referrals/consent.js (dark) mints an application and emails two realtors; admin/logout.js deletes the caller's own admin session.

Write routes without a per row ownership function: every route that relies on `authorizeApplicant` or a listing SELECT through the session client (add-applicant, applicants, invite, report-pdf, report-text, send-report, referrals assign and create, clear-analysis, manage-analysis, request-documents, doc-request-status, verify-confirm-pdf and text). Their ownership rests on the listings policy returning a row only for the owner. lib/realtorRoute.js:3 to :4 states the standard is an explicit compare "never RLS"; these routes do not meet it. Whether the policy exists in production is UNKNOWN (2d).

### 2a. Every token

ID_ALPHABET has 31 symbols (lib/applicationIds.js:11), 4.954 bits per symbol; base64url of N bytes is 8N bits.

| Token | Generator | Entropy | TTL and enforcement | Opens | Appears in | GET changes state? |
|---|---|---|---|---|---|---|
| Owner token | newOwnerToken, pick(32) with crypto.randomInt (lib/applicationIds.js:25, :27) | 158.5 bits | none on the token; the `app:{RL}` record lives 1 year (pages/api/generate.js:31 to :32); revoke flag (manage.js:164 to :170) | POST application/manage (view, update, prefill, revoke, unrevoke), tenant/sync-application, profile link-application; all constant time compares | the generate response (:320); the confirmation email body and its link (pages/api/send.js:105, :110); localStorage on the device (pages/apply/[token].js:356, pages/my-application/[rl].js:19, :65); the signed in tenant's profile GET (lib/tenantProfileStore.js:287 to :294); the applications column, stripped on every realtor read (lib/supabaseBridge.js:112, lib/realtorUpload.js:28, lib/pipeline.js:39, :107). Logs: none found | the tenant pages have no getServerSideProps; the token moves from the query into localStorage (pages/my-application.js:76) then POSTs view. Read only. The paired application number carries 39.6 bits |
| Invite token | crypto.randomBytes(10).hex (pages/api/listings/invite.js:54), reused unless regenerate (:53) | 80 bits | 90 days on KV (:109 to :112), refreshed on reuse; the listings column never expires (:117); resolve reads KV (resolve.js:29) so the link dies with the KV record | /apply/{token}; GET invite/resolve; POST generate, invite/tag, applications/mirror, pipeline/consent | the route response, listings.invite_url, the short link target, the pipeline invite email. Logs: pages/api/pipeline/consent.js:43 passes the full token to logServerError on the 500 path | the apply page getServerSideProps (:33 to :43) calls readPrefill only; read only |
| Short code | newShortCode, 5 symbols with crypto.randomInt (lib/shortLink.js:17) | 24.8 bits (28,629,151 codes) | 90 days on `short:{code}` (invite.js:100); old key deleted on regenerate (:96) | 302 to /apply/{inviteToken} (pages/a/[code].js:18), disclosing the 80 bit invite token to whoever enumerates a live code; no limiter in that handler (:11 to :21); platform limits UNKNOWN | the route response and the post kit texts | read only (kvGet only, :16) |
| Document request token | newDocReqToken randomBytes(16).hex (lib/docRequest.js:36) | 128 bits | 7 days on `docreq:{token}` and the pointer (:25, :106 to :107); the pointer alone is refreshed by a nudge (lib/nudges.js:110) | /upload/{token}; GET upload/resolve; POST upload/analyze-file and finalize (write the realtor's bucket and junction row) | the request email and response (request-documents.js:69, :111); the mirror response (mirror.js:75); the confirmation email (send.js:169, :188); the nudges; the realtor status response (doc-request-status.js:43 to :44). Logs: none | the upload page has no getServerSideProps; resolve is read only |
| Document open | none. documents/open takes a documentId under the realtor session and answers a Supabase signed URL (open.js:39); the signing key is Supabase's, entropy UNKNOWN | n/a | 60 seconds | one object fetch | JSON response (:46), then an img or iframe src (components/dashboard/DocumentViewer.js:32, :34) | POST only; counts the open (:43) |
| Report snapshot token | newReportToken, pick(32) (lib/applicationIds.js:29); column unique (db/report-snapshots.sql:16) | 158.5 bits | 90 days into expires_at (lib/reportSnapshot.js:18, lib/reportSnapshotStore.js:16, :21); enforced at pages/r/[token].js:35, report/answer.js:36, report/pdf.js:21 | /r/{token}; GET report/pdf; POST report/answer | the landlord email (send-report.js:21, :79); the realtor client through snapshotMeta (reportSnapshotStore.js:30, send-report.js:98). Logs: first 6 characters only (answer.js:46, pdf.js:30) | YES. pages/r/[token].js:36 calls noteOpened, which bumps opened_count and last_opened_at (reportSnapshotStore.js:70) and records report_opened at most hourly (:73). Email scanners register as landlord opens. The PDF GET is read only |
| Consent token | newConsentToken randomBytes(24).base64url (lib/listingStatus.js:18); minted at listings/status.js:72 (pending) and pipeline/consent.js:39 (consented) | 192 bits | 60 days into expires_at (lib/listingState.js:5, :47); enforced by readConsent (:28) and flipConsent (:45) | /keep/{token}; POST pipeline/answer | the not selected email (status.js:77, listingState.js:66, :75). Logs: pipeline/answer.js:44 passes the full token to logServerError on the 500 path | read only (pages/keep/[token].js:15 to :38, two selects) |
| Renew token | newConsentToken (lib/pipeline.js:149); column unique (db/pipeline.sql:16) | 192 bits | valid only while the row is consented and unexpired (lib/pipeline.js:169), so at most 7 days from send; cleared on answer (:179) | /keep/{renewToken}; POST pipeline/answer falls through to answerRenewal (:37) | the renewal email. Logs: same answer.js:44 | read only (readRenewal, :164 to :173) |
| Reference token | newReferenceToken randomBytes(24).base64url (lib/referenceStore.js:9); validator 8 to 128 chars (:10) | 192 bits | 14 days into expires_at (lib/referenceQuestions.js:15, referenceStore.js:29); enforced at :41 and :55 | /ref/{token}; POST references/answer (writes answers, status and confirmations.landlord_reference, :59, :64) | the reference email (request.js:34). Logs: references/answer.js:42 passes the full token to logServerError on the 500 path | read only (pages/ref/[token].js:16 to :33) |
| Prefill token | newPrefillToken, 32 bytes mapped `b % 31` (lib/pipeline.js:20) | modulo bias, 4.952 bits per symbol, 158.5 bits | 14 days in KV (lib/pipelineState.js:12, pipeline.js:89); deleted only by POST pipeline/prefill after a submission (:110); not consumed by reading | /apply/{inviteToken}?from={prefill} renders the form filled from the application row (owner_token and cover_letter stripped, :107); readable repeatedly until submitted or expired | the pipeline invite email; the query string of a GET URL (browser history and referrers: GUESS). Logs: pages/apply/[token].js:45 message only | read only (readPrefill, :101 to :109), but the response is the person's application as server rendered HTML |
| Cron bearer CRON_SECRET | not generated in the repo | UNKNOWN | none; compared with `!==` (lib/documentStore.js:157) | the four cron routes | the Authorization header; never logged (:155 logs only that it is unset) | the cron GETs are the state changing runs in 2c |
| Tenant magic link | newToken randomBytes(32).base64url (lib/tenantProfileStore.js:62), stored as sha256 under `tmagic:` (:61, :202) | 256 bits | 15 minutes (:37); single use (:205 to :212) | GET tenant/verify?t= | the email (request-link.js:28 to :31); a dev only console.warn of the full URL when RESEND_API_KEY is unset outside production (:33) | YES: consumes the token, creates a 30 day session, sets the cookie, 302 (verify.js:7 to :14) |
| Tenant session cookie rl_tenant | newToken (:214), sha256 under `tsession:` (:215) | 256 bits | 30 days in KV and cookie Max-Age (:38, :228); not refreshed on use; destroyed on sign out (:218) | tenant/profile, tenant/prefill | Set-Cookie HttpOnly SameSite=Lax; Secure only when NODE_ENV is production (:225 to :230) | the profile GET is read only |
| Tenant email change token | newToken (:242), sha256 under `temail:` | 256 bits | 60 minutes (:39); single use (:246 to :253) | GET tenant/verify-email-change?t= | the email (profile.js:135, :138); a dev only console.warn (:139) | YES: re keys the profile (verify-email-change.js:6 to :10) |
| Referral consent token (dark) | newToken hashed under `rconsent:` (lib/referrals.js:158 to :159) | 256 bits | CONSENT_TTL | /refer/{token}, GET and POST referrals/consent | the referral email | GET returns the applicant's facts; POST mints the application |

Cross cutting: three public answer routes log a full credential on their 500 path (pipeline/answer.js:44, pipeline/consent.js:43, references/answer.js:42) while the report routes truncate to 6 characters; the landlord page is the one token page whose GET writes; the short code's 24.8 bits is the weakest secret and it resolves to the 80 bit invite token; the prefill token exposes an applicant's full application to any holder for 14 days without being consumed on read.

### 2b. Every email

Send sites: pages/api/send.js:184, pages/api/applicants/request-documents.js:95, lib/nudges.js:108 (from the nudges cron), pages/api/listings/status.js:81, pages/api/listings/send-report.js:84, pages/api/pipeline/invite.js:31, lib/pipeline.js:155 (from the pipeline cron), pages/api/references/request.js:35, pages/api/tenant/request-link.js:31, pages/api/tenant/profile.js:138, and the four referral emails (pages/api/referrals/create.js:40, consent.js:19), dark while lib/features.js:9 is false.

| Email | Sender | reply_to | Links and what a GET only scanner does | Dash punctuation | Protected ground or score | Footer |
|---|---|---|---|---|---|---|
| Tenant confirmation (send.js) | Rentletter <hello@rentletter.ca> (:185) | none | upload link (only when it matches the 32 hex shape, :170 to :171) opens /upload/[token], read only; rentletter.ca/my-application (:99), read only; the manage link with app and token (:110), read only until the page POSTs | YES: :93 "Apply in seconds &mdash; open your profile" and :99 "a fresh link &mdash; no password needed"; :130 signs off ", The Rentletter desk" with a leading comma | none; carries first name, application number and the owner token | "Good luck out there." then ", The Rentletter desk" then "Ontario · Not legal advice" (:127 to :148): its own template |
| Document request (request-documents.js) | Rentletter <hello@rentletter.ca> (:96) | realtor (:98) | upload link (:69, :88) opens /upload/[token], read only | none | none; tenant name, address, realtor name. Aside: :89 says documents are "analyzed to verify income, employment, and credit", which contradicts the positioning rule in CLAUDE.md | "This link is private to you and expires in 7 days." (:90) under a black Rentletter bar (:82); no on behalf line although the body speaks for the realtor (:86) |
| Nudge one and two (lib/nudges.js) | {realtor} via Rentletter (:43) | realtor profile email (:108) | upload link (:65), read only | none | none; first name and unit | "Sent through Rentletter on behalf of {who}. This link is private to you." (:67) |
| Not selected (lib/listingState.js from status.js) | {realtor} via Rentletter (:52) | realtor (status.js:81) | keepUrl and declineUrl, the same URL (:54, :75 to :76), open /keep/[token], read only; the answer is a POST | none | none by design (:49); first name only | "Sent through Rentletter on behalf of {who}." (:77) |
| Landlord report (send-report.js reportEmail) | {realtor} via Rentletter (:22) | realtor (:85) | the landlord page /r/{token} (:21, :43): this GET writes opened_count and a report_opened event (pages/r/[token].js:35 to :36), so scanner opens count as landlord opens; the page's PDF link is read only | none | body carries a count only (:33); the attached PDF carries every applicant's Fit number and label (lib/landlordReportPdf.js:188 to :189) | "Sent through Rentletter on behalf of {name}. Applicant data is self reported; verify references independently." (:45) |
| Invite to apply (lib/pipelineState.js inviteEmail) | {realtor} via Rentletter (:85) | realtor (invite.js:31) | /apply/{inviteToken}?from={prefill} (invite.js:24): the GET is read only but its response is the server rendered form with the person's previous application (email, name, phone, income, employer, previous landlord contact, co applicant name, age, relationship and income, pets, reference names and contacts, lib/pipelinePrefill.js:11 to :32); anyone holding the link for 14 days receives that HTML | none | none in the email | "Sent through Rentletter on behalf of {who}." (:97); `who` falls back to a capitalised "Your realtor" (:104) where the nudges and not selected use lower case |
| Renewal (lib/pipelineState.js renewalEmail) | {realtor} via Rentletter (pipeline.js:155) | realtor profile email | /keep/{renewToken}, read only | none | none | same as above, same capitalisation note (:115) |
| Reference request (lib/referenceQuestions.js) | {realtor} via Rentletter (:67) | realtor (request.js:35) | /ref/{token}, read only | none | none; carries the applicant's full name to a third party (:74) and the brokerage in the signoff (:77) | "Sent through Rentletter on behalf of {who}." (:88) |
| Tenant magic link (lib/tenantEmails.js) | Rentletter (request-link.js:31) | none | /api/tenant/verify?t= (:28): the GET consumes the token, creates a 30 day session for the opener and redirects (verify.js:1 to :14); the tenant who clicks afterwards sees "That link has expired or was already used" (pages/my-application.js:77) | YES: lib/tenantEmails.js:42 "you can ignore this email &mdash; nothing changes" | none | "Rentletter · Ontario & BC · rentletter.ca" (:19) |
| Tenant email change (lib/tenantEmails.js) | Rentletter (profile.js:138) | none | /api/tenant/verify-email-change?t= (:135): the GET completes the change (verify-email-change.js:6 to :10); the human at the new address never confirms | none | none; carries the old address (:55) | same tenant shell (:19) |
| Referrals, four emails (lib/referralEmails.js), dark | Rentletter, no reply_to (create.js:40, consent.js:19) | none | /refer/{token} whose GET returns the applicant's shared facts including "Household & pets" with an occupant count (lib/referrals.js:91 to :101); /landlord#referrals; /signup?ref=1 | YES: referralEmails.js:23, :33, :41, :51 each carry an em dash | A12 names "household" (:23); no score | "Rentletter · Ontario & BC · rentletter.ca" (:9) |

Three sender shapes exist: Rentletter with no reply address (confirmation, magic link, email change, referrals), Rentletter with the realtor as reply address (the document request only), and {realtor} via Rentletter with the realtor as reply address (the six realtor voiced messages). The document request is the outlier that speaks for the realtor without the via Rentletter sender or the on behalf footer. The confirmation says "Ontario" where the tenant shell says "Ontario & BC". Links whose GET changes state: the magic link, the email change link, and the landlord report page.

### 2c. The crons

Gate: cronGate (lib/documentStore.js:153 to :159): 503 when CRON_SECRET is unset (the log line names `[cron/expire-documents]` for all four crons, :155), 401 when the header is not `Bearer {secret}` (:157, plain compare). All four are GET with maxDuration 60. That Vercel sends the bearer is platform behaviour (GUESS from the route comment at expire-documents.js:1 to :3).

| Route | UTC | Toronto EDT | Toronto EST | Deletes or sends | Twice in a day | A week without running |
|---|---|---|---|---|---|---|
| expire-documents (03:00) | 03:00 | 23:00 the day before | 22:00 the day before | every live document row past expires_at: object then row, batches of 100 up to 50 batches (lib/documentStore.js:128 to :146), one documents_expired event per applicant per batch (:143) | idempotent: the select filters deleted_at IS NULL (:129) and the update sets it (:135) | nothing lost; the next run takes everything past expiry, capped at 5000 rows |
| retention (03:30) | 03:30 | 23:30 the day before | 22:30 the day before | dry run unless RETENTION_ENFORCE=true (pages/api/cron/retention.js:19): dry run logs the first batch and the ten oldest numbers and deletes nothing (lib/retention.js:83, :92); enforce deletes junction rows then applications older than twelve months by created_at in batches of 100 (:85 to :87); the 90 day closed listing selection is logged only (:90 to :91) | dry run logs twice; enforce is idempotent | dry run logs; enforce catches up. The retention_run event is never recorded (1b) |
| nudges (13:00) | 13:00 | 09:00 | 08:00 | reads the docreq-pending set (lib/nudges.js:78); nudge one at 48 hours, nudge two at 5 days, never a third (:36 to :40); drops on report present, withdrawn, set aside, listing not open, missing pointer (:27 to :33, :102) | guarded by pointer.nudgedAt (:36 to :40, appended :110): one nudge per applicant per run, but no minimum gap, so nudge two can follow nudge one on the next run when the request is already 5 days old | a catch up run sends only nudge one (:39 checks sent.length first). After 7 days the record and pointer have expired (lib/docRequest.js:25, :106 to :107), the pointer reads null, selectNudge drops the member (:27): a long outage cancels reminders rather than bunching them. If the pointer write after a send fails (kvSetJson returns false, docRequest.js:72, :77) the same nudge repeats next run |
| pipeline (13:30) | 13:30 | 09:30 | 08:30 | flips pending and consented rows past expires_at to expired (lib/pipeline.js:129, :137 to :141); sends one renewal to consented rows with 1 to 7 days left and no renew_sent_at (:124 to :128), recording pipeline_renewal_sent (:157) | idempotent: renew_token and renew_sent_at are written before the send (:151) | rows that passed expires_at during the gap are expired with no renewal; rows with days left get theirs. A failed send is logged and never retried (:158) because the guard is written first |

### 2d. RLS, every table

From db/*.sql. Whether any of it was run in production is UNKNOWN; db/schema-reference.sql:2 says it is reference only and :61 dates its DDL export to 4 September 2026.

| Table | Created in | RLS enabled | Policies | Session client use | Admin client use | Notes |
|---|---|---|---|---|---|---|
| profiles | NOT FOUND (only ALTERs: db/admin-suspend.sql:12, billing-and-promos.sql:18 to :23, founder-renumber.sql:11, notifications-last-seen.sql:13, onboarding.sql:17 to :20, stripe-lifecycle.sql:15 to :18) | NOT FOUND in db/ (GUESS on from the two policies in the export comment) | profiles_select_own and profiles_update_own, `id = auth.uid()`, comment export only (db/schema-reference.sql:145 to :146); no INSERT policy; the row is created by a trigger outside the repo (founder-renumber.sql:17 to :18) | reads: pages/landlord.js:22 and :62, pages/landlord/[id].js:21, pages/billing.js:19, pages/onboarding.js:21, pages/profile.js:24, lib/realtorAuth.js:11, lib/requireEntitlement.js:12, lib/listingReportData.js:89 and :155, lib/notificationsFeed.js:29, listings/invite.js:42, branding/generate-logo.js:157, request-documents.js:58, billing/checkout.js:20, billing/portal.js:15. Writes: lib/profileBackfill.js:12 (from pages/landlord.js:57), notifications.js:32 | lib/adminData.js, lib/billing.js, lib/promos.js:64, lib/nudges.js:92, lib/pipeline.js:146 and :171, lib/listingStatus.js:31, lib/referenceStore.js:43, lib/referrals.js:155 and :207, lib/realtorWrites.js:155, mirror.js:72, use-logo.js:77, tenant/profile.js:71 | base columns (full_name, brokerage, email, logo_url, brand columns, signup_number, is_founder, subscription_status) appear in no db/ file |
| listings | NOT FOUND (comment DDL at schema-reference.sql:68 to :103; ALTERs at listing-status.sql:16 to :18) | NOT FOUND (GUESS on) | listings_all_own FOR ALL `profile_id = auth.uid()`, comment export only (:143) | browser: components/dashboard/HomeView.js:134; session: pages/landlord.js:43, pages/landlord/[id].js:43, lib/dashboardSignals.js:29, lib/notificationsFeed.js:22, lib/listingReportData.js:152, lib/applicantAnalysis.js:28, listings/invite.js:34 and :117 (write), listings/applicants.js:25, add-applicant.js:38, pipeline/people.js:13, referrals/assign.js:18, referrals/create.js:27 | lib/supabaseBridge.js:151, lib/events.js:35, lib/listingStatus.js:11, lib/nudges.js:87, lib/ownApplicant.js:11, lib/realtorUpload.js:20, lib/realtorWrites.js:106 to :143, lib/retention.js:38, mirror.js:54, resolve.js:42, status.js:49, pipeline/consent.js:36, upload/analyze-file.js:77, upload/finalize.js:70 | the invite route's :117 update is the last session write on the realtor path |
| listing_applicants | NOT FOUND (comment DDL at :106 to :124; ALTERs at reviewed-at.sql:17, listing-applicants-vocabulary.sql:16, screening.sql:16 to :17); UNIQUE(listing_id, application_id) exists only in the manual script db/verification-dedupe.sql:80 to :82 | NOT FOUND (GUESS on) | listing_applicants_all_own through the listing's profile_id, comment export only (:144) | none | every read and write in lib/supabaseBridge.js, lib/realtorWrites.js, lib/realtorUpload.js, lib/referenceStore.js, lib/retention.js, lib/nudges.js, lib/pipeline.js, the applicants routes, status.js, send-report.js, finalize.js | lib/dashboardAdapter.js:35 declares a browser read of this table that no component performs |
| applications | NOT FOUND (comment DDL at :127 to :137; ALTERs at employment-income.sql:14 to :18, profile-edits.sql:13 to :15, referrals.sql:43 to :44, tenant-profiles.sql:39 to :40) | UNKNOWN: schema-reference.sql:148 says "no policy: reads and writes go through the service role only" and does not say RLS is enabled; no db/ file enables it | none (:148) | none | lib/supabaseBridge.js:20 (upsert), every embedded `application:applications(*)`, lib/pipeline.js, lib/retention.js, lib/tenantProfileStore.js, confirm.js:50, references/request.js:23, tenant/profile.js:59 | holds owner_token and cover_letter (:137). If RLS is off in production, the anon key that ships to the browser (lib/supabase/client.js:13) can read every application. Resolve by running `select relrowsecurity from pg_class where relname = 'applications'` |
| applicant_documents | db/documents.sql:22 | :44 | service role only (:43) | none | lib/documentStore.js, lib/retention.js:77, documents/open.js:31 and :43 | FK to listing_applicants ON DELETE CASCADE (:24); profile_id has no FK (:25) |
| events | db/events.sql:14 | :74 | events_select_own FOR SELECT TO authenticated `profile_id = auth.uid()` (:85); writes revoked (:76, :78) | pages/api/events/index.js:14 (no profile filter; the policy is the scope) | insert lib/events.js:21; read lib/dashboardSignals.js:36 | the type constraint is rewritten by eight files; each guard tests one marker type, so the effective list depends on run order (UNKNOWN which ran) |
| event_reads | db/events.sql:66 | :75 | event_reads_select_own (:88); writes revoked | pages/api/events/index.js:16 | upsert events/read.js:11 | |
| promo_codes, promo_redemptions | billing-and-promos.sql:34, :59 | :69, :70 | service role only; ALL revoked (:71 to :72) | none | lib/promos.js; the SQL function redeem_promo_code (:108, EXECUTE only to service_role :134 to :135) | |
| stripe_events | stripe-lifecycle.sql:28 | :35 | service role only | none | lib/billing.js:51 | |
| crm_brokerages, crm_leads, crm_notes | crm.sql:10, :19, :46 | :62 to :64 | service role only | none | lib/crmStore.js | founder admin only |
| pipeline_consents | listing-status.sql:31; columns added pipeline.sql:15 to :17 | :46 | service role only (:14) | none | lib/pipeline.js, lib/listingStatus.js, lib/retention.js:53, status.js:74, pipeline/consent.js:39 | profile_id has no FK (:33) |
| reference_responses | reference-responses.sql:14 | :30 | service role only (:12) | none | lib/referenceStore.js | profile_id has no FK (:17) |
| referrals | referrals.sql:16 | :40 | service role only | none | upsert only, lib/referrals.js:68; never read | write only mirror; KV is canonical (:10 to :11) |
| report_snapshots | report-snapshots.sql:12 | :29 | service role only (:27) | none | lib/reportSnapshotStore.js, report/answer.js:40 | profile_id has no FK (:15) |
| tenant_profiles | tenant-profiles.sql:20 | :36 | service role only (:35) | none | lib/tenantProfileStore.js, lib/adminData.js:48 | stores per application owner tokens (:25, :33) |
| bucket applicant-documents | documents.sql:17 | n/a; no storage policies for anon or authenticated (:15 to :16) | none | none | lib/documentStore.js, documents/open.js:39 | |
| bucket logos | NOT FOUND in db/ | n/a | UNKNOWN | browser writes with the anon key session: upload and remove under `${user.id}/logo.*` (components/dashboard/ProfileEditorBody.js:108 to :109, :131), public URL at :111 | use-logo.js:60 to :73, lib/adminData.js:196 to :197 | nothing in the repo constrains a signed in user to their own folder; the browser write test at tests/realtorWrites.test.mjs:108 to :110 matches `.from(table)` writes and not `storage.from(...).upload` |

Browser or session reads that depend on a policy: every profiles read above depends on profiles_select_own; the profile writes at lib/profileBackfill.js:12 and notifications.js:32 depend on profiles_update_own; every listings read and the invite route's update depend on listings_all_own; pages/api/events/index.js:14 and :16 depend on the two events policies plus the SELECT grants (db/events.sql:80 to :81); the logos bucket writes depend on a storage policy that is not in the repo.

What a signed in realtor could read that is not theirs: no policy uses `true`; the only CREATE POLICY statements in db/ are the two in events.sql. The exposure is conditional on production state: if listings_all_own is missing with RLS on, every listing read returns nothing and every write route answers 404 or 403; if RLS is off on listings or applications, the anon key reads everything. Two masks make either state hard to notice: pages/landlord.js:24 treats a null profile with no error as a fresh account and redirects to onboarding (:67), and lib/requireEntitlement.js:13 evaluates entitlement on `{ id }` when the row is null and answers 402; pages/api/events/index.js:21 to :24 turns any error into an empty timeline; lib/notificationsFeed.js:29 swallows the profile read error.

Tables referenced in code that no db/ file creates: profiles, listings, listing_applicants, applications (DDL exists only as the comment export), and the logos bucket. db/verification-dedupe.sql:38 contains a stray `1` on its own line between two statements, which fails if the file is pasted whole.

### 2e. Held documents

Bucket `applicant-documents` (lib/documentStore.js:18), created private with a 10MB limit and a JPEG, PNG, PDF allowlist by db/documents.sql:17 to :19 as `ON CONFLICT DO NOTHING`, so a pre existing bucket keeps its own settings (dashboard state UNKNOWN). Path `{profile_id}/{listing_applicant_id}/{uuid}.{ext}` (lib/documentStore.js:44 to :46), no filename, no applicant field, no owner token. Table applicant_documents, RLS on with no policies (db/documents.sql:22 to :44), FK to listing_applicants ON DELETE CASCADE (:24).

Signed URL: pages/api/documents/open.js:39 `createSignedUrl(path, 60, { download: false })`, 60 seconds, behind session (:21), entitlement (:24), a live row (:33), ownedApplicant and a second profile_id compare (:35 to :37). Every open bumps opened_count and last_opened_at (:43) and records document_opened (:45), with two caveats: a failed counter update is logged and the URL is still returned (:44, :46), and recordEvent returns false rather than throwing (lib/events.js:22, :25), so an open can be served unlogged when either write fails.

Download: no Content-Disposition on the document response; the only attachment headers in the repo are on PDF report routes. The signed URL is returned as JSON (:46) and placed in an img or iframe src (components/dashboard/DocumentViewer.js:32, :34); `#toolbar=0&navpanes=0` is a viewer hint only. Anything holding the URL for its 60 seconds can fetch and save the object. "Never downloadable" is a UI property (DocumentViewer.js:2), not a server one.

Delete paths.

| Path | Code | Storage object | Row | On storage failure |
|---|---|---|---|---|
| Realtor delete | documents/delete.js:35, purgeStoredDocuments (lib/documentStore.js:72 to :82) | removed (:77) | deleted_at, deleted_by realtor name (:79) | remove error logged only (:78); the row is still marked. File with a deleted row |
| Reanalyze, tenant path | upload/analyze-file.js:104 to :105 with replace on the first file, then documentStore.js:91 to :94 (deleted_by reanalyze) | removed | marked | same |
| Reanalyze, realtor path | lib/realtorUpload.js:63 passes `files: [{ mime, data, name }]` | the previous files are purged when replace is true (documentStore.js:91 to :94) | previous rows marked | LIVE BUG: storeAnalyzedDocuments skips any file without `bytes` (documentStore.js:97) and the realtor path passes base64 `data`. Every realtor upload since 717bdd0 purges the applicant's held files and stores nothing: the report writes, the held list comes back empty, and the "Documents held" section disappears. The tenant path passes `Buffer.from(data, 'base64')` as `bytes` (analyze-file.js:105) and works. tests/documents.test.mjs:59 and :89 exercise `bytes` only; tests/hygiene.test.mjs:29 to :75 injects a fake `store` and never sees the shape mismatch. The sandbox route (lib/demoAdapter.js:243 to :266) builds its own held list. Verified by reading both lines; not run against Supabase |
| Expiry cron | expire-documents.js:20, expireDocuments (documentStore.js:125 to :149) | removed per batch (:133) | deleted_by expired (:135) | remove error logged only (:134); rows still marked. Orphan file, no retry, no reconciliation |
| Listing delete | listings/delete.js:9, deleteListing (lib/realtorWrites.js:132 to :146) | never touched | rows vanish by cascade (listings to listing_applicants to applicant_documents) | files with no row, silently |
| Applicant withdrawal | withdrawApplicant (lib/realtorWrites.js:67 to :85) | untouched | untouched | held until expiry; consistent |
| Retention enforce | lib/retention.js:85 to :86 | never touched; live rows counted and logged (:77 to :84) | cascade | files with no row |
| Insert failure rollback | documentStore.js:107 to :112 | uploaded object removed (:109) | never inserted | no orphan |

Every marking path writes the row after the storage call and ignores the storage error, so a failed remove always yields a file with a deleted row. Listing delete and retention enforce yield files with no row. No path yields a live row with no file, except that the realtor upload bug above yields no rows and no files where the realtor believes documents are held.

## Part 3. Compliance, every surface

### 3a. Field by surface

Surfaces: card head (components/dashboard/ListingView.js:751 to :816), card body (:819 to :842; incomeRows :693 to :701, tenancyRows :702 to :708, livingRows :709 to :713), Compare (components/dashboard/CompareTenants.js:31 to :46, pool at ListingView.js:615 to :634), checklist (components/dashboard/ScreeningChecklist.js:95 to :102, answers :132), People (components/dashboard/PeopleList.js:56, :82 to :83, :90; rows lib/pipelineState.js:59 to :73), landlord page (pages/r/[token].js:125 to :143 from lib/reportSnapshot.js:51 to :71), PDF (lib/landlordReportPdf.js:184 to :194), text (lib/reportText.js:16 to :19), emails (2b). Y* means the cell sits beside a Fit score, a grade or a rank. Every card body and checklist cell is Y* because the Rank pill (:822) and the header score (:759 to :766) stay visible while the card is open; every Compare cell is Y* because rows 32 and 33 are Rank and Overall grade.

| Field | Card head | Card body | Compare | Checklist | People | Landlord page | PDF | Text | Emails |
|---|---|---|---|---|---|---|---|---|---|
| Full name | Y* :757 | Y* | Y* :136 | Y* :96 | Y* :63, :82 | Y* :60, :125 | Y* :187 | Y* :18 | full name in the reference and document request emails; first name elsewhere |
| Email | N | N | N | N | Y* when no name (pipelineState.js:63) | N | N | N | old address in the email change mail |
| Phone | N (the duplicate line says "Same phone as {name}" without the number, lib/duplicates.js:41) | N | N | N | N | N | N | N | N |
| Age, date of birth, age_confirmed | N | N | N | N | N | N | N | N | N. The date never leaves the device (pages/apply/[token].js:339 to :340); the flag sits in scorecard jsonb (lib/applicationMap.js:66); no realtor component reads it |
| Income (gross) | Y* as a multiple of rent (lib/applicantSynthesis.js:42, :65); the dollar figure when a minimum criterion is set (lib/fitScore.js:144) | Y* :694 | Y* :34 | Y* :97 | N | Y* :63, :137 | Y* :193 | N | N |
| Net income | N | Y* :695 | N | N | N | N | N | N | N |
| Employer | N | Y* :697 | Y* :38 | Y* :98 | N | Y* :62 and the sentence :68 | Y* :190 | Y* :18 | N |
| Job title | N | Y* :698 | N (parsed for employment type only, CompareTenants.js:17 to :22) | Y* :98 | N | Y* sentence | Y* | Y* | N |
| Employment type | N | Y* :697 | Y* :39 | N | N | N | N | N | N |
| Years at job | Y* when a tenure criterion is set (fitScore.js:145) | Y* :699 | Y* :37 | N | N | Y* :63, :137 | Y* :193 | Y* (reportSentence.js:8) | N |
| Previous address | N | N | N | N | N | N | N | N | N (read by Fit, fitScore.js:116) |
| Years at previous | N | Y* :704 | Y* :40 | N | N | N | N | N | N |
| Previous landlord name and contact | N | Y* :706 | N | Y* :99 with the email at :138 | N | presence only, "Landlord reference on file" (reportSnapshot.js:57, :64) | presence only | presence only | the reference email goes to the contact but does not print it |
| Current rent | N | Y* :703 | Y* :41 | N | N | N | N | N | N |
| Move in date | N | Y* :710 | Y* :43, the earliest gets the green leader tick (:58 to :73, :90) | N | N | N | N | N | N |
| Occupants and details | N | N | N | N | N | N | N | N | N. Stored (lib/applicationMap.js:54 to :55). The set aside reason label "Occupants exceed the unit maximum" (lib/setAsideReasons.js:12) shows beside the Rank pill on an opened set aside card (ListingView.js:745, :822, :832), and the set aside modal copy lists "occupancy" among screenable facts (:1267) |
| Smoker | N | Y* :711 | Y* :44 | N | N | N | N | N | N |
| Pets | N | Y* :712 | Y* :45 | N | N | N | N | N | N |
| Co applicant name, age, relationship, employer | N | N | N | N | N | N | N | N | N rendered; the whole co_applicant object reaches the browser (ListingView.js:617, :687) and the prefill form (lib/pipelinePrefill.js:28) |
| Co applicant income | N | Y* :696 "Household income … (joint, before tax)" | Y* :35 | compared through fit.incomeUsed (ScreeningChecklist.js:69) while "Said" prints the applicant's own income (:97) | N | Y* silently: the landlord's Income is fit.incomeUsed, household income, with no marker (reportSnapshot.js:63; fitScore.js:90, :151) | Y* same | N | N |
| References | count only in the synthesis line | Y* count :707 | Y* count :42 | Y* count :100 | N | Y* count :63, :137 | Y* count :193 | N | N; names never render on a realtor surface |
| Fit score and label | Y :759 to :766 | Y | Y :32 to :33 | rent share only (:101) | Y :56, :90 | Y :128 to :129 | Y :188 to :189 | Y :17 | report email body carries a count (send-report.js:33); the attached PDF carries every score |
| Scorecard v2 | N | N | N | N | N | N | N | N | N; nothing reads it (1a) |
| Reason line (fitReason) | N | N | N | N | N | Y :135 | Y :192 | N | N |
| Fact sentence (reportSentence) | N | N | N | N | N | Y :133 | Y :190, :291 | Y :18 | N |
| Confirmations | Y* "Verified by {who} · date" :780 | Y* | N | Y* :152, :168 | N | Y :134 | Y :191, :293 | N | N |
| Reference answers | N | Y* through the checklist :132 | N | Y* :132 | N | N; only "previous landlord (by email)" in the confirmed line | N | N | N |
| duplicateOf | Y* "Same phone / email / name and employer as {name}" | Y* | N | N | N | N | N | N | N |

Protected grounds or proxies still landing beside a score or rank: the occupancy set aside label beside Rank (above; GUESS: the closest thing to a family status proxy on a ranked surface); the "(joint, before tax)" label beside the score discloses that a co applicant exists (ListingView.js:696); co applicant age and relationship are never rendered but ride in the client payload and in the server rendered prefill HTML for 14 days; smoker and pets (unit rules) sit beside rank and grade on Compare with no leader tick; move in date (not a ground) is the one non financial Compare row that gets a leader tick; the referral facts sheet (dark) returns an occupant count.

Two things beside the number that are not grounds but are worth knowing: the landlord's Income figure is household income with no marker, and an emailed reference adds 0.3 to R whatever it says (fitScore.js:79, :121), including "often late", "significant" damage and "would not rent again".

Builder inputs. reportSentence (lib/reportSentence.js:5): jobTitle, employer, yearsAtJob, rentSharePct, landlordReference boolean; called once from lib/reportSnapshot.js:68. fitReason (lib/fitScore.js:175): two computeFit results; reads scoreExact, E, evidence.hasReport, employerConfirmed, edited, contradicted, A, incomeCapped, ratio, parts.tenure, landlordRef, tenancy, refs (:176 to :199); emits at most two of ten fixed phrases (:184 to :203) and never the income level (:169); printed as "Below the one above: …" (pages/r/[token].js:135, lib/landlordReportPdf.js:192).

### 3b. Fit

lib/fitScore.js computeFit (:70 to :152) reads, from the application: annual_income (:86), co_applicant.annualIncome or annual_income (:89), prev_landlord_name (:115), prev_address (:116), references length (:117), years_at_job (:118), years_at_previous (:119), profile_updated_at through editedAfterReport (:78). From the listing: monthly_rent (:82), pref_rent_to_income_max_pct (:98), pref_min_annual_income (:102), pref_requires_employer_verification (:109), pref_min_years_at_job (:125), pref_requires_landlord_reference (:127). From the verification report through readVerification: state, incomeMatched, incomeFound, employerMatched, nameMatch, incomeMismatch, employerMismatch (:54 to :68, :83). From confirmations: id, employer, landlord, landlord_reference, reference (:79, :122). Nothing else. No free text field is read (test d at tests/fitScore.test.mjs:35 passes different reason_for_moving and disclosures and asserts identical objects). The tests cover each pref cap (:41 to :69), the label rules (:101, :146), the confirmations (:81 to :99, :138), the edited state (:211) and the sort (:164).

The People list calls the same function with verification null and the original junction row's confirmations (lib/pipelineState.js:51), and the test at tests/pipeline.test.mjs:66 to :75 asserts the list's best.score equals computeFit's score for the same inputs. tests/references.test.mjs:97 to :119 asserts four answer sets give one scoreExact and that the file never reads an answer field.

### 3c. The retention promise against the crons

| pages/privacy.js | Promise | Enforced by | Gap |
|---|---|---|---|
| :25 | "held in private storage for 14 days, or until the realtor deletes it, whichever comes first" | expire-documents cron on expires_at set from RETENTION_DAYS 14 (lib/documentRetention.js:4); realtor delete route | enforced; a failed storage remove leaves the object with a deleted row (2e) and nothing reconciles |
| :25 | "through a short lived link that expires within one minute" | 60 second signed URL (documents/open.js:39) | enforced |
| :25 | "every view and deletion is logged" | opened_count plus document_opened (open.js:43, :45); document_deleted on delete | mostly: an open whose counter or event write fails is still served (open.js:44, :46) |
| :25 | "never shared with landlords or other realtors" | the landlord page and PDF carry no documents; ownership checks on open | enforced |
| :25 | "removed from storage when they expire or are deleted" | expireDocuments removes the object (documentStore.js:133) | enforced when the remove succeeds; see the orphan paths in 2e (listing delete and retention enforce never remove objects) |
| :52 | Anthropic processing "in the United States", documents "not retained" by the processor | UNKNOWN from the repo (a vendor term) | not verifiable here |
| :60 | application data "retained while the related listing is active and for up to twelve months after the last activity" | retention cron: dry run by default (retention.js:19); when enforced, deletes by created_at older than twelve months (lib/retention.js:23, :28), not by last activity, and regardless of whether the listing is active | not enforced today (dry run); when enforced, the rule differs from the promise in both directions: an application edited last week but created 13 months ago is deleted; an application created 11 months ago on a listing rented 10 months ago is kept. The 90 day closed listing rule in the code (:35) is logged only and is not in the policy |
| :61 | "Extracted factual results are retained subject to the same retention period as the related application" | doc_verifications lives on the junction row, which the retention delete removes (retention.js:85) | same dry run gap |
| :62 | realtor account data "retained while the account is active and for a reasonable period after account closure" | only the founder's admin delete (pages/api/admin/realtors.js) removes an account; no self service closure exists | not enforced by any cron; UNKNOWN whether a process exists outside the code |
| :64, :73 | earlier deletion on request | manual | not enforced by code |

Enforced and not promised: the 90 day rule for applications on rented or closed listings (lib/retention.js:35, logged only), the 60 day consent expiry (lib/listingState.js:5), the 14 day reference request and prefill token lifetimes, the 90 day report snapshot lifetime (lib/reportSnapshot.js:18). The 90 day snapshot means a landlord's page and PDF stay retrievable for 90 days with every applicant's name, income and score, which the policy does not mention.

## Part 4. What a realtor would hit, in order

Headless Chrome, 390 by 844, against the sandbox on a local dev server. Screenshots: /tmp/walk-00-signup.png through /tmp/walk-25-keep.png. Not iOS Safari. The sandbox skips every session, entitlement, ownership and rate limit check by construction, so nothing below says anything about production behaviour.

Realtor.

1. Sign up (/signup, walk-00). Email, password, confirm password, a Province select (Ontario, British Columbia) with "Where you operate. You can change this later in your profile.", the terms checkbox, "Create account →". Says twice: the province is asked here and again by the listing modal when the profile has none (pages/signup.js:200, components/listings/ListingSetupModal.js askProvince). Nothing wrong in the copy. The arrow in "Create account →" is the only glyph.
2. Dashboard, fresh profile (/demo/dashboard?profile=fresh, walk-01). Header, "Good afternoon, Sarah.", the red New listing, the ink Next list with three rows and "5 more", Your listings with two cards, People with three rows. Unclear: the bell badge reads 8 while Next shows three rows plus "5 more"; a new realtor does not know the bell and Next are the same list. Copy: "7 applicants · 1 verified · 2 to check · report not sent" on the Carlaw card is the state line; the sandbox pair makes it 7 where the listing page says 6 (the listing page counts active rows only, lib/listingStateLine.js versus the applicants header at ListingView.js). Two taps where one would do: nothing here.
3. New listing (walk-02, walk-03). The modal opens on Province (required, first), then Address, Monthly rent, Bedrooms, Pets, Smoking, Parking, the OHRC note, landlord name and email, preferences. "Still needed: Landlord name." updates as fields fill. The header copy "Fields marked * are required: address, monthly rent, bedrooms, and your landlord client's name and email" does not mention the province even when the province is the first required field. The harness could not complete the create (the landlord name input was not matched by placeholder), so the create confirm and the first listing page were not captured; the create confirm is covered by the earlier session's screenshots of the rent warning.
4. Post kit (walk-05). "Post kit" fold on the listing header: SHORT LINK, DESCRIPTION, INSTAGRAM BIO, SAVED REPLY, each with Copy, then QR with "Save QR". The fold is collapsed by default (ListingView.js:966 uses aria-hidden and inert), which is right; the innerText capture reads it anyway. Copy: "Apply in ten minutes, no PDFs to attach" and the header "Copy" beside the long invite URL sit one above the other, so a realtor sees two links (the long apply URL and the short link) with no line saying they are the same door.
5. The listing page (walk-06). Header card with the criteria pills, the Copy link, Post kit and Details folds; then "Applicants 6" with the state line "1 verified · 2 to check · 1 waiting on documents · 2 no documents", the swipe tip, six cards, Compare, Set aside (1). Each card: name, meter, score, label, one line, one action. Twice: Sofia Russo appears twice with "Same phone as Sofia Russo" on the second, as designed. Unclear: "2 to check" on the state line covers a CHECK DOCS card that says "Documents on file · nothing matched" and one that says "Profile edited Sep 5 · analyse again"; three wordings for one bucket (1c). The red primary on David's card is "Review documents" while the page's one red action is meant to be New listing on the dashboard; on this page the red button is the card action, which matches the one red rule per screen.
6. The card and the checklist (walk-07). Priya expanded: "4.8 VERIFIED", "Verified income at 2.9x rent, landlord reference on file", "Verified by you · Sep 5", Rank 1, Finalist, the application number, INCOME AND EMPLOYMENT, TENANCY AND LANDLORD REFERENCE, LIVING SITUATION (empty for her), SCREENING CHECKLIST with six rows, Document verification ✓ done, the request documents card, Set aside and Withdrew. Twice: "Confirmed · Sep 5" appears on Income and on Employer because both rows share the employer key (ScreeningChecklist.js:73 to :74, sameAsAbove); a realtor reads two confirmations for one call. Copy: "Use a number you find yourself, not one the applicant gave." appears under Employer and under Previous landlord, twice on one screen. The LIVING SITUATION heading renders with nothing under it when the applicant has no pets and no smoker value (ListingView.js:824 gates on livingRows length, so this is the "Pets: None" default row keeping the section alive; walk-07 shows the heading followed directly by the checklist).
7. A confirmation (walk-08). Tapping "Called a reference" turns it to "Confirmed · Sep 7" at once; the header stayed VERIFIED. One tap. Nothing to undo except tapping again, which the aria label explains.
8. Ask by email (walk-09). The control sits after "Confirmed · Sep 5" on the Previous landlord row; tapping opens the in row box "Six closed questions go to gail.mercer@email.com. Nothing goes to Priya." with Send and Cancel; Send turns the row to "Asked Sep 7 · no answer yet". Two taps by design. Unclear: on Priya the landlord is already "Confirmed" by a call and the email ask is still offered; nothing says what a second confirmation adds.
9. Send report (walk-10 to walk-12). Present to landlord: "Signed by: Sarah Chen Change", "Landlord client", Generate PDF, Copy text for landlord, Email report. On a fresh profile Email report opens "Sign the report as" with the name prefilled and Send; after it the block reads "Sent to Marco Rossi · Sep 7 · opened 0 times · 0 answers", "View as landlord", the branding line, and the sandbox notice. Twice: "Signed by: Sarah Chen" above and "Sign the report as: Sarah Chen" in the sheet on the same screen; the sheet exists to write the stored name, but to the realtor it asks a question the block already answered. Copy: the Generate PDF button is ink filled while Email report is also ink filled once a snapshot exists, so two filled buttons sit side by side (ListingView.js:1189, :1197).
10. The landlord page (walk-13). Realtor name and brokerage, the address, "Prepared September 7, 2026 for Marco Rossi", six cards with rank, name, score, label, the sentence, the confirmed line, "Below the one above: …", four numbers, "I'd like to meet them" and "Not for me", the criteria footer, Download PDF. Wrong: Sofia Russo appears twice, ranks 5 and 6, with two different employer strings, because the duplicate mark never reaches the snapshot (lib/reportSnapshot.js:51 to :71 has no dedupe); the landlord sees two people. Seam: David's confirmed line reads "Confirmed by the realtor" while Priya's reads "Confirmed by Sarah Chen" (1c). Copy: "Below the one above: documents did not match" on a card labelled CHECK DOCS says the same thing twice in two vocabularies.
11. An answer (walk-14). Tapping "I'd like to meet them" on rank 1 shows "You want to meet them" with a red tick; the other cards keep their two controls. One tap. Nothing says the answer went to the realtor.
12. Mark as rented (walk-15, walk-16). The control is an ink outlined 44px button under the applicants; the sheet "Who got it?" lists the active applicants and an outside option with a notify toggle, then Confirm. After it the header shows "Rented · Sep 7" and the not selected emails are described in the sandbox notice. Two taps by design. UNKNOWN in the sandbox: whether the harness's Confirm landed (walk-16 shows the header state; the log line for the sheet was empty because the sheet's role is alertdialog and the harness queried it after the tap).
13. The not selected email. Not renderable in the sandbox; the copy is at lib/listingState.js:59 to :64 and is quoted in 2b. Nothing wrong found in it.
14. People and an invite (walk-17, walk-18). Three rows; tapping Daniel Osei opens two listing lines with Invite on each; Invite becomes "Invited Sep 7" and the row line gains "· invited Sep 7". One tap to invite, which is right; nothing asks "are you sure" before an email leaves, which is a choice worth knowing.

Tenant.

15. The apply form (walk-20, /apply/demo0000000000000001). The ink "You're applying to" banner, "Tell us about you", the privacy note, seven sections, 21 inputs, 5 selects, 1 textarea, no input under 16px (the harness checked every field), "Review & submit" disabled until the vital fields pass, "Still needed before you can submit: Full name, Date of birth (18+), Valid email, 10-digit phone, Income before tax, Employer, Job title, Move in date." The page is 9,078px tall at 390 wide (walk-20 full page). Says twice: the banner says "The unit details above were entered by the listing realtor, you only need to tell us about yourself below" and the intro says "the unit details are already filled in by the listing realtor, so just tell us about you", back to back. Copy: "10-digit phone" and "Self employed (own or family business)" and "Full-time", "Part-time" carry hyphens in labels (pages/apply/[token].js: the vital labels at :898, the employment options at :718 to :722); "Household & pets" is a section title with an ampersand. The Date of birth field says "You must be 18+ (Ontario age of majority) to apply on your own." which is the right sentence.
16. The prefilled form (walk-21, ?from=demoprefill). "Check it, then submit.", "Filled from your application from before. Edit anything that changed.", six groups with Edit, one Submit. Wrong: "Age of majority: not set" for Nadia, because the sandbox prefill row carries no scorecard.ageConfirmed (lib/demoFixture.js demoPeopleSource builds rows without it), so Submit expands the form and asks the date. In production the flag comes from the stored row (lib/pipelinePrefill.js:13); a person whose original application predates 717bdd0 has no flag and hits the same wall. That is a real case, not only a sandbox one.
17. The rented link (walk-22). "This unit has been rented.", "Want Sarah Chen to keep you in mind for similar units?", an email field, "Yes, keep me in mind", "No account is created. Your email is kept for 60 days for this purpose only." Right.
18. The number, the document card, the confirmation email. Not reachable in the sandbox: /api/generate writes to KV and the local KV credentials are invalid, and the sandbox does not intercept the apply page. The document card code is components/tenant/DocumentUploader.js mounted at the last step of the apply page; the email is 2b. UNKNOWN on a phone until a real submission is made against staging.
19. The upload page (walk-23, /upload/demo…0002). "This link is no longer active", "This upload link is not valid.", "Go to Rentletter". The sandbox token the dashboard shows for Marc's request (lib/demoAdapter.js docRequests) does not resolve on the tenant side, so the sandbox's own link is dead. A realtor testing the product from the dashboard's request card will hit this.
20. The tenant profile page (walk-24, /my-application). "Your rental profile, in one place.", the email field, "Email me a link →", "Have an application number and owner key instead?", the "Haven't applied anywhere yet?" note. Copy: "we'll send you a link · no password" uses the middle dot as a comma; fine. Nothing wrong.
21. The keep page (walk-25, /keep/demo-open). "Sarah Chen asks:", "Keep your application in mind for similar units for 60 days?", "Yes, keep me in mind", "No thanks". Right; the renew variant reads "for another 60 days?".

## Part 5. Performance, the user side

Numbers from the local dev server (unminified, no CDN, warm) for the sandbox pages, and from the last production build log (scratchpad build of fd583dd, `npm run build` not rerun for this audit). Server time against production Supabase is UNKNOWN: no session exists locally; the only server timing available is the loadSignals trace on the fake client.

| Page | Requests | Bytes (dev) | Fonts | TTFB (dev) | DOMContentLoaded (dev) | CLS | First Load JS (production build) |
|---|---|---|---|---|---|---|---|
| Dashboard (/demo/dashboard) | 11 | 115,896 of which fonts 115,820 | 2, no duplicates | 273 ms | 563 ms | 0.009 | /landlord 204 kB; /demo/dashboard 396 kB |
| Listing page | 12 | 115,896 | 2 | 211 ms | 500 ms | 0.035 | /landlord/[id] 361 kB |
| Landlord page (/r/DEMO-demo-carlaw) | 11 | 115,896 | 2 | 234 ms | 454 ms | 0 | /r/[token] 107 kB |

Fonts: fraunces-latin.woff2 67,388 bytes and inter-latin.woff2 48,432 bytes, self hosted, loaded once each on every page. No image loaded twice; no font loaded twice. Layout shift: 0.035 on the listing page, under the 0.1 threshold; GUESS from the capture that the shift is the applicants list filling after the client fetch of /api/listings/applicants.

The dashboard server load: 9 Supabase queries and 1 KV call at depth 2 on the loadSignals fixture (tests/loadSignals.test.mjs trace), down from 54 before bff7c13. On production the same load adds the pipeline_consents probe and, when rows exist, two more reads (lib/pipeline.js:31 to :44), so 9 to 11. The wall time of that load against Supabase is UNKNOWN.

Bundles: the shared runtime is 95.7 kB (framework 57.5 kB, main 37 kB). /landlord/[id] at 361 kB is the largest realtor page; what is in it is UNKNOWN without a bundle analysis, but the imports at components/dashboard/ListingView.js:8 to :42 name the candidates: qrcode-generator (the QR in the post kit), CompareTenants, ApplicantDocIntel, ApplicantDocRequest, ScreeningChecklist, DocumentViewer, ListingSetupModal, ReferModal and ReferralCaution (dark behind the flag but still bundled), the motion module, and the report snapshot and PDF text helpers. /demo/dashboard at 396 kB carries both views plus the fixture. /billing, /signin, /signup, /forgot-password and /reset-password sit at 171 to 191 kB for pages that are one form; GUESS: the Supabase browser client is the weight.

The three slowest things a phone on cellular would notice: (1) the listing page bundle, 361 kB of JavaScript before the applicants list can hydrate, on a page whose server render already contains the list; (2) the two fonts, 116 kB, loaded before the display face renders any heading, on every page including the landlord page and the apply form; (3) the apply form itself, a 9,078px document with 27 controls delivered as one page, where every keystroke re renders the form (pages/apply/[token].js keeps the whole form in one useState at :54) and the review step is a modal at the very bottom.

## Part 6. The first audit's list

| # | Finding (docs/audit-2026-09.md Part 6) | Status | Closed by | What is left |
|---|---|---|---|---|
| 1 | longTermIntent and disclosures keywords in the ranking | closed | d3ef69b (scorecard v2), then 915d68c replaced the scorecard with Fit on every realtor surface | the scorecard is still computed and stored (1a) |
| 2 | age, date_of_birth, co_applicant.age and relationship collected and stored | partly | 717bdd0 stopped writing age and date_of_birth and stores age_confirmed | co_applicant.age and relationship are still written by pages/api/generate.js:279 and lib/tenantProfile.js:227 and stored in co_applicant jsonb; the homepage form still asks Age and Date of birth (pages/index.js:839 to :840) |
| 3 | occupants, smoker, pets beside the score; pref_max_occupants | partly | 717bdd0 removed occupants from Compare and the card; pref_max_occupants left the code by 1f30d8c | smoker and pets remain beside rank and grade on Compare (CompareTenants.js:44 to :45) and beside the score on the card (ListingView.js:711 to :712); the occupancy set aside label sits beside Rank (3a) |
| 4 | locked out by a masked profile read failure, or by a missing plan column reading as none | partly | e363e7f (the profile read failure) | lib/entitlements.js:21 still reads a missing plan as none and locks; no probe for db/billing-and-promos.sql having run; the masks in 2d remain |
| 5 | realtor upload over 4.5MB fails at Vercel | closed, and reopened differently | 717bdd0 (per file upload) | the per file path stores nothing (2e); the size problem is fixed, the storage is broken |
| 6 | the ranking ignores the stated criteria; verification does not enter the score | closed | 915d68c, 7e09a4e, 1f3bbc8 (Fit with pref caps, evidence and record) | none |
| 7 | reports built live, never frozen | closed | 18eaf0b (report_snapshots, the landlord page, answers), ae44daa (the PDF from the payload) | the duplicate applicant reaches the snapshot twice (Part 4, step 10) |
| 8 | browser side writes bypass the entitlement gate | closed | 1f30d8c (every realtor write behind a route), 4f5626d | the invite route still updates the listing row through the session client (pages/api/listings/invite.js:117); the logos bucket is written from the browser (2d) |
| 9 | referral acceptance fails until db/listing-applicants-vocabulary.sql widens added_via | moot | c919627 froze referrals behind lib/features.js:9 | the code path still writes ADDED_VIA.REFERRAL (lib/referrals.js:267) with no runtime guard; UNKNOWN whether the SQL ran |
| 10 | the bell reruns the full load on every page; 46 queries per view; inboxFor writes during reads | closed | bff7c13 (seven queries), c90262b (the action list), lib/signalsCache.js (60 second cache) | the cache is per instance (1b) |
| 11 | the name match gate fails hyphenated and single name applicants with no override | closed | 1607228 (the checklist; Saw ID sets confirmations.id, which readVerification honours as ignoreName at lib/fitScore.js:58 to :60) | none |
| 12 | TOP 5 pills and copy at low volume | closed | ae44daa removed the last "TOP 5" string; no such string remains in components or lib | none |
| 13 | verif-trace logs drown errors; the 401 on send and the null listing read are unlogged | closed | bff7c13 removed the trace lines; logServerError is used on the routes | UNKNOWN whether the specific 401 branch on send-report logs; not re verified |
| 14 | rent parsed by regex from the description | closed | 717bdd0 (rent from the invite record) | estimated_rent is null when the invite record has expired from KV (1b) |
| 15 | verify at apply | closed | 29cdc54 (documents at apply, nudges) | the sandbox's own upload link is dead (Part 4, step 19) |
| 16 | a landlord page instead of a PDF | closed | 18eaf0b, ae44daa | none |

## Part 7. The ranked list

| # | Finding | Type | Impact | Effort | Files | Blast radius |
|---|---|---|---|---|---|---|
| 1 | pages/api/send.js is a public, unauthenticated, unlimited email sender from hello@rentletter.ca; applicationNumber and ownerToken are interpolated into the HTML unescaped and unchecked (:68, :105, :110, :171) | SECURITY | 5 | S (require a fresh generate result or a signed payload; escape and format check every field; rate limit by IP) | pages/api/send.js, pages/apply/[token].js:395, pages/index.js:290 | domain reputation, phishing under the brand |
| 2 | RLS state of applications is unknown; the anon key ships to the browser and the table holds owner_token and cover_letter; the four core policies exist only in a reference comment | SECURITY | 5 | S to verify (one query per table in the SQL editor), M to fix if wrong | db/schema-reference.sql:143 to :148, lib/supabase/client.js:13 | every applicant's data |
| 3 | The realtor per file upload purges the applicant's held documents and stores nothing: lib/realtorUpload.js:63 passes `data`, lib/documentStore.js:97 requires `bytes` | BUG | 5 | S (Buffer.from(data, 'base64') as bytes, plus a test that calls the real store with a fake admin) | lib/realtorUpload.js, lib/documentStore.js, tests/hygiene.test.mjs | every realtor upload since 717bdd0 |
| 4 | pages/api/upload/analyze-file.js has no limiter and no cap on staged files: a docreq token holder can run unlimited vision calls and bucket uploads for 7 days | SECURITY | 4 | S (MAX_DOCS check as in lib/realtorUpload.js:46 and :54, plus checkSubmitLimits) | pages/api/upload/analyze-file.js | Anthropic and storage bills |
| 5 | pages/api/invite/tag.js and pages/api/applications/mirror.js are public writes with no limiter; tag accepts any RL string and rewrites the invite record without an expire; mirror inserts an applicant_applied event on every call | SECURITY | 4 | S (checkSubmitLimits on both; validate the RL with lib/applicationIds.js and confirm `app:{RL}` exists; keep the TTL) | pages/api/invite/tag.js, pages/api/applications/mirror.js | KV growth, timeline spam, invite expiry |
| 6 | The prefill token renders a person's full application as HTML to any holder for 14 days without being consumed; co applicant age and relationship ride in it | COMPLIANCE | 4 | S (consume on first render into a one time session cookie, or shorten to 48 hours and strip co applicant fields) | lib/pipeline.js:101 to :110, pages/apply/[token].js:33 to :43, lib/pipelinePrefill.js:28 | applicant privacy |
| 7 | Three public answer routes log the full credential on their 500 path | SECURITY | 3 | S | pages/api/pipeline/answer.js:44, pages/api/pipeline/consent.js:43, pages/api/references/answer.js:42 | log readers |
| 8 | The magic link and the email change link are consumed by a GET; a mail scanner logs in as the tenant or completes the change | SECURITY | 3 | M (an interstitial page with one POST button, as /keep and /ref already do) | pages/api/tenant/verify.js, pages/api/tenant/verify-email-change.js | tenant accounts |
| 9 | The occupancy set aside label sits beside Rank; smoker and pets sit beside rank and grade on Compare; the landlord's Income is household income with no marker | COMPLIANCE | 3 | S (move the set aside reason off the ranked card; drop smoker and pets from Compare or move them under a unit rules heading with no rank beside them; label joint income on the report) | lib/setAsideReasons.js:12, components/dashboard/ListingView.js:745 and :822, components/dashboard/CompareTenants.js:44 to :45, lib/reportSnapshot.js:63 | defensibility |
| 10 | co_applicant.age and relationship are still collected and stored | COMPLIANCE | 3 | S (stop writing them; the form keeps asking nothing it does not store) | pages/api/generate.js:279, lib/tenantProfile.js:227, pages/apply/[token].js | applicant data |
| 11 | /api/pipeline/people always answers an empty list (ctx.admin is never set) | BUG | 4 | S (getSupabaseAdminClient() in the route) | pages/api/pipeline/people.js:14, lib/realtorAuth.js:12 | the People card on the client retry path |
| 12 | A duplicate applicant reaches the landlord page and PDF twice | BUG | 4 | S (skip rows with duplicateOf in buildSnapshot, or fold them) | lib/reportSnapshot.js:51 to :71 | every report with a double application |
| 13 | The invited prefilled form asks for the date of birth again whenever the stored row predates the flag, and the sandbox shows the same wall | BUG | 3 | S (treat a stored application as confirmed, since it was accepted under the same check) | lib/pipelinePrefill.js:13, pages/apply/[token].js:228, lib/demoFixture.js demoPeopleSource | every invite to a pre 717bdd0 applicant |
| 14 | Nudge two can carry a dead upload link: the record TTL is 7 days from mint and is never extended | BUG | 3 | S (extend `docreq:{token}` on each nudge) | lib/nudges.js:110, lib/docRequest.js:106 | late second nudges |
| 15 | The retention cron's event can never be recorded, and the retention rule (created_at, twelve months, any listing) differs from the policy (last activity, active listing) | COMPLIANCE | 3 | M (decide the rule, then a table or an admin scoped event) | lib/retention.js:23, :28, :94, pages/privacy.js:60 | the privacy promise |
| 16 | Deleting a listing or enforcing retention leaves storage objects with no row; a failed storage remove leaves objects with a deleted row; nothing reconciles | BUG | 3 | M (remove objects in deleteListing before the cascade; a weekly reconcile that lists the bucket against the table) | lib/realtorWrites.js:132 to :146, lib/documentStore.js:77 to :79, :133 to :135 | storage growth, the 14 day promise |
| 17 | The invite record (90 day KV TTL) and the listing row disagree on the invite's lifetime; resolve reads KV, so an old but live listing's link answers "expired" and estimated_rent goes null | BUG | 3 | M (resolve from the row; keep KV for counters) | pages/api/invite/resolve.js:29, pages/api/listings/invite.js:102 to :117, pages/api/generate.js:191 | listings older than 90 days |
| 18 | The short code carries 24.8 bits and resolves to the 80 bit invite token with no limiter on the redirect | SECURITY | 2 | S (a limiter on /a/[code], or 7 symbols) | lib/shortLink.js:17, pages/a/[code].js:11 to :21 | invite enumeration |
| 19 | The confirmation and referral emails carry em dashes; the confirmation signs off with a leading comma and says Ontario only | UX | 2 | S | pages/api/send.js:93, :99, :130, :148, lib/tenantEmails.js:42, lib/referralEmails.js | copy rules |
| 20 | The document request email says documents are analyzed to verify "credit" | UX | 2 | S | pages/api/applicants/request-documents.js:89 | positioning |
| 21 | Five label vocabularies for one state across the card, the state line, Next, the report and the checklist; two names for one realtor on one landlord page | SEAM | 2 | M (one label map in lib/applicantState.js consumed everywhere; pass the realtor's name to confirmedSummary for reference confirmations) | lib/listingStateLine.js:17, lib/actions.js:51 to :55, components/dashboard/ScreeningChecklist.js:65 to :67, lib/reportSnapshot.js:26 | reading comprehension |
| 22 | Fourteen realtor routes rest ownership on the listings policy instead of an explicit compare, against the standard at lib/realtorRoute.js:3 to :4 | SECURITY | 2 while the policy exists, 5 if it does not | M (ownedListing in each) | see the route table | depends on item 2 |
| 23 | The People card is invisible when the profile probe fails and the client retry answers empty (item 11), and the five most used paths have no behaviour test | BUG | 3 | M (route level tests with fake clients for landlord.js, listings/create and invite, generate plus mirror, landlord/[id], send-report) | tests/ | every future change to those paths |
| 24 | age_confirmed lives in scorecard jsonb and survives only through kvAppToRow | SEAM | 2 | S (a boolean column) | lib/applicationMap.js:66, lib/pipelinePrefill.js:13, lib/tenantProfile.js:120 | future writers of the row |
| 25 | The province is asked at sign up and again in the modal; the modal's header copy does not name it | UX | 1 | S | pages/signup.js:200, components/listings/ListingSetupModal.js | first run |
| 26 | The apply form is a 9,078px page with 27 controls in one state object and the review at the bottom | PERF | 2 | L (steps, or at least a sticky progress row) | pages/apply/[token].js | tenant completion rate, UNKNOWN without analytics |
| 27 | Dead weight: lib/scoring.js, lib/scorecard.js, the scorecard half of lib/deriveScorecard.js, clear-analysis, the pass routes, two unimported components, the homepage form's Age and Date of birth fields, the write only referrals table | DEAD | 1 | S | see 1a | none |
| 28 | Sandbox tokens are handled inside production routes and pages, and the ?profile=fresh switch lives in the adapter | SEAM | 1 | M | 1b | none |

Items 1 to 10 are security and compliance and rank first regardless of effort; 2 costs one query in the SQL editor and decides whether 22 is a two or a five. Items 11 to 14 are bugs a paying realtor sees this week: 11 hides People after any client side reload, 12 puts a duplicate in front of a landlord, 3 loses every uploaded document. Everything after is ordered by impact over effort.
